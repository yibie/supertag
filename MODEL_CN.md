# Supertag 信息模型

- Status: 决定沿革与目标说明
- Date: 2026-09-04
- Scope: Supertag 处理信息的模型：事物、状态、动作、边界。不涉及实现顺序。
- Relation: 与 `doc/OWNERSHIP-CONSTITUTION_cn.md` 的差异见第 8 节。

> 2026-09-06 检点提示：本文混有原提案、后续决定与历史实现说明，不可整篇直接作为删除清单。尤其第 4、6、8 节的“数据库整体可重建／冲突与备份可删除”，已被用户后续的文档事实 `file → database` 澄清收窄。下列检点标记仅标出需对齐之处，不代表代码清理或迁移已经完成。


## 阅读说明

- 本文保留产品讨论及旧实现描述；“未实施”“已完成”等按其记录时间理解，
  不以单句旧进度覆盖后来的验收。
- 文档事实单向 `file → database`，不等于整个Store可丢弃。
  非重建事实、旧配置、冲突、备份和恢复不能用reindex代替或删除。
- §4/§6/§8中“数据库全部是缓存”“冲突不存在”等原提案已被 M1 取代；
  旧tag/schema/ontology实现不反向约束新默认模型，也不据此清除历史数据。

下文是带上述适用性限制的原提案／决定沿革；不得直接用作删除清单。

## 数据流检点标记（2026-09-06）

来源：用户要求先检点模型和数据流，再做有证据的删除／封装清理。本文保留历史决定原貌，当前适用性按下表辨别。

| 标记 | 对照位置 | 需对齐的内容 | 处理与验证边界 |
|---|---|---|---|
| M1 已确认纠偏 | §4 两个引擎、§6.3、§8 数据库与冲突 | file → database 约束笔记事实的权威与更新，不表示整个数据库是缓存。重建文档投影不替代非重建事实的备份、恢复或冲突处理。 | 不得依据旧文字删除持久事实、数据库保存／恢复能力或冲突模块；逐集合／消费者确认归属。 |
| M2 已确认纠偏 | §3 动作、§9 Automation | 规则由用户写在 Emacs 配置里；数据库负责匹配／定位，执行修改 Org，再投影。纯 reindex 不执行或积压待执行的 Automation。 | 不引入独立规则文件，不以“单向数据流”为由禁止数据库查询后执行文档动作，也不据此删除规则存储／索引／调度。 |
| M3 数据流需封装 | §4 活 buffer、保存与同步 | 活 buffer、已保存磁盘文本、数据库投影须区分；“文件曾保存”不能证明重试读取的 live buffer 仍是已保存版本。 | 盘点原生保存、内部受控保存、增量同步与重建各入口；明确持久投影的版本依据。不得把全部同步强行改成一个 hook，也不新增第二个 writer。 |
| M4 数据流需封装 | §3 Promote、§7 精确写入计划 | 一次用户操作跨目标／旧位置／当前选区；阶段曾完成不保证其依赖在 retry 时仍安全，不能用已有关系或节点 ID 推断该次操作完成。 | 以 Promote 安全修复为先，覆盖后续阶段原 payload 重试。全流程安全依据与资源生命周期须对齐；不承诺跨文件原子事务或进程崩溃恢复。 |
| M5 修改范围需核对 | §3 光标／选区、§9 返回原节点与 Stream | 选区范围与选择方向分开；文本位置不等于内容身份；可见范围不等于取消归属；返回原视图不等于恢复整帧旧布局。 | 优先局部文本编辑和局部显示恢复。保留原生导航，不以新增通用上下文框架代替明确修改范围。已验收行为保留为回归约束。 |
| M6 已确认纠偏 | §2 概念、§9 概念文件／模板 | 监视资格为“持久 Org ID ＋ 当前模板目标文件”，不取决于由谁创建。无 ID 标题不监视；确认身份相关操作才补 ID。最后一个模板退出旧文件时只停止监视。 | 读取／预览／取消零写；不扫描补 ID，不移除历史标记，不删除退出监视的文件或链接。 |
| M7 删除／封存需证据 | §7、§8、命令清单 | 删用户入口、删内部实现、封存默认接入、迁移旧数据是不同操作。代码名称陈旧或没有静态调用，均不足以证明可删。 | 每个候选记录运行／动态／配置／持久化消费者、保留理由、替代落点及反证测试。封存源码保留，必要读取／恢复／兼容路径不连带删除。 |
| M8 明确未完成／未决 | §2 标签与关系、§8 旧字段、§9 实现说明 | 标签名字／路径切换、旧配置退出、未知协议冷发现及其余原计划里程碑没有因清理自动完成；“当前没有 Semantic Edge”是历史用户陈述，不是当前真实库扫描。 | 不据此迁移真实笔记、清除旧事实或擅定冷发现／旧配置策略。历史“尚未实施”说明逐条与验收证据对齐，保留历史来源。 |

检点退出条件：上述每项有明确适用规则、实现／消费者证据和“改／删／封装／保留／未决”结论；进入清理的每项有精确范围及可反证测试。尚未完成这份实现盘点，标记本身不构成任何删除授权。

一句话：

> 人起名，机器记名，人释义。

展开：节点只有状态。判断类状态由人设，抄录类状态由人审，系统让状态的后果看得见。系统主动显示你起过的名字出现在哪里；打开 node view 也算召唤，在其中展示当前节点的相关笔记，供阅读与写作参考。

## 1. 它相信什么

一个笔记工具的模型，由它要求用户反复做的那一个动作定义。

| 工具 | 最小单位 | 中心动作 | 结构发生时机 |
|---|---|---|---|
| Roam、Logseq、Obsidian | block 或文件 | 写的时候打 [[ | 写时 |
| Tana、Capacities | 类型化对象 | 先定型再填字段 | 写前或写时 |
| Supertag | Org 标题 | 重读自己写过的散文，选中一个说法，宣布它是概念 | 读时 |

Supertag 的立场：

- 写作时零结构。散文就是散文，不要求标签、字段、链接。
- 结构在阅读时长出来。你重读自己的文字，给一个说法起名，系统从此记住这个名字，并告诉你它在哪里出现。
- 概念同一性的判断由人完成，别名由人手写。打开 node view 时可以通过语义检索找到不同措辞的相关材料；检索候选不等于概念相同，也不自动成为链接。
- 机器不替人确认意义，不催、不计债。用户打开 node view 时提供相关笔记，采用哪些材料由人决定。
- 三个月后用户手里多出来的，是一本从自己散文里长出来的词典。每个词是自己挑的，每条释义是自己写的，每一处出现都能追溯到原文。

## 2. 事物

只有一种事物：节点，即一个带 ID 的 Org 标题。ID 由 org-id 生成，是不带含义的 UUID。节点是什么类型看标签，不看 ID，也不看标题前缀。笔记和概念不是两种东西，是节点的两种状态。

| 状态 | 怎么设 | 落在 Org 哪里 | 系统拿它做什么 |
|---|---|---|---|
| 已提升 | 用某条 promote 模板提升选区或标题 | 该模板的概念文件里多一个标题，带模板规定的标签 | 监视这个名字，显示它在哪出现 |
| 别名 | 手写 | 属性 | 一并监视 |
| 已链接 | 确认一次提及，或手写链接，或在光标处插一条带关系的链接 | Org 文本里的 [[id:...]] 或 [[关系名:...]] | 算 backlink 与共引；带关系名的链接投影成有向带标签的边 |
| 有标签 | 打 #tag | 标题上的 #tag | 投影成集合 |
| 有正文 | 写 | 正文 | 无 |
| 有元数据 | 召唤 LLM 提出，人审阅接受 | PROPERTIES 抽屉 | 投影成表 |
| TODO | Org 原生 | Org 原生 | 投影成看板 |

每一行互相独立。提升不要求正文，链接不要求提升，标签不要求任何东西。系统不从一行推出另一行，也不对任何一行的空缺发表意见。

三个定义：

- 概念：名字被监视的节点。promote 的操作含义就是"从现在起盯着这个名字"。
- 提及：一个被监视的名字在某个节点正文里的一次出现。用户可以链接一处，或链接当前节点中同一概念的全部提及；不保留节点范围的忽略命令。候选是一次性读模型，不入库；只有确认后写进 Org 的链接才是事实。旧 SUPERTAG_IGNORE_MENTIONS 属性的兼容处理待实施时核实，不在命令去留讨论中删除现有数据。
- 标签：人手贴在节点上的标记，用来把一批同类节点归成集合。它没有 schema。节点的字段就是它的 Org 属性抽屉，不由标签规定。标签和概念的区别只有一个：标签是贴上去的，概念是从正文里被提到的。

### 边

边只用 Org 链接表达：源节点，链接类型（关系名），目标节点。类型即关系名，
链接写在谁的节点文本里，谁就是源：

```org
* Smith 2019 的 n-back 实验 #evidence
:PROPERTIES:
:ID: 7c0e5b2a-3d91-4f6e-b8a4-1e9d2c5f0a37
:END:
被试连续两晚只睡 4 小时后 n-back 正确率下降 18%，
这[[supports:2f1c7e3a-8b4d-4c1e-9a6f-0d2b5e7c9a11][支持睡眠不足损害工作记忆]]，
但样本只有 24 人。
```

属性键不派生关系名。例如
`:SUPPORTS: [[id:2f1c7e3a-8b4d-4c1e-9a6f-0d2b5e7c9a11]]` 仍是普通
`SUPPORTS` 属性；其中的 `id:` 链接只按普通链接契约读取，不能因容器名被改写成
`supports` 边。普通属性以及属性中的链接值不因此失效或改义。目标节点视图里的
"supported by"是从带类型链接派生的显示，永不写回文本。Discourse Graph 把类型
放在标题前缀里，这里放在链接类型里，标题保持自然语言，ID 保持无含义。

- 动作在 Org buffer 里，光标处或选区上。统一使用 supertag-add-link（由 reference-insert 能力合并改名）：默认插入普通 id 引用，需要时指定关系名，插入带类型链接；普通引用不增加必选关系步骤。有选区则选区成为链接文字。删除独立的 supertag-link-add、supertag-link-menu、supertag-link-remove 入口；解除关系通过编辑对应的 Org 链接完成。节点视图只显示出边和入边，不在里面操作。
- 关系名不预先配置也能用。补全候选是全库已经用过的关系名，加上用户可选配置的一份词表。输入新词就是新关系。和 TODO 关键字一样，系统不校验用法。
- 让 Org 认识这些链接类型：Supertag 对每个在用的关系名调用 `org-link-set-parameters`，注册成 `id:` 的同类，跟随、导出、org-element 解析都按原生走，类型原样保留，不需要链接缩写。
- 未注册也未在用的链接类型不是边，只是普通 Org 链接，Supertag 不碰。
- 不声明语法。数据库投影一张"关系名 × 源标签 × 目标标签"的用量表，语法是描述出来的，不是校验出来的。写 `[[supports:...]]` 指向一个 `#question` 不会被拒绝，只会在表里多一行。
- 反向标签只是显示问题。默认入边用同一个词加方向显示；需要"supported by"这类措辞时另配一张仅供显示的对照表，可选。
- 数据库里的 `:relations` 记录是带类型链接的缓存：从 Org 链接解析而来，节点重新解析时整体替换，删了库能重建。禁止任何只写数据库不写文本的建边入口。
- 边是"已链接"状态的子类，不是新状态。节点类型就是标签，例如 `#claim`、`#evidence`、`#question`。

## 3. 动作

所有动作发生在光标处或选区上。

| 动作         | 输入                     | 文本变化                                                      | 谁决定           |
|--------------|--------------------------|---------------------------------------------------------------|------------------|
| 写           | 散文                     | 无额外变化                                                    | 人               |
| promote      | 选区，或经确认选中的已有节点 | 选区提升在原处写 Org 链接，不搬移所在标题；复用概念文件外已有节点时，节点及正文移入模板目标文件，原位置留下引用。统一入口按选择键使用用户模板，常用模板可配置直接命令和快捷键 | 人               |
| 链接         | 一条候选提及             | 这一处文字变成 [[id:...]]                                   | 人               |
| 链接节点内全部提及 | 一个概念在当前节点内的候选提及 | 当前节点中对应的候选文字全部变成 Org 链接 | 人 |
| 别名         | 一个概念                 | 属性多一行                                                    | 人               |
| 标签         | 一个节点                 | 标题多一个 #tag                                             | 人               |
| 手写链接     | 任意位置                 | [[id:...]]                                                  | 人               |
| 带关系的链接 | 光标处或选区 | Org 文本里 [[关系名:ID][文字]]；关系名从已用过的链接类型里补全 | 人               |
| 召唤         | 一个节点，或一个标签集合 | LLM 提出候选，人逐条接受后才写入                              | LLM 提出，人审阅 |

判断类动作，即起名、链接、别名、标签，由人亲手做。抄录类动作，即元数据抽取，由 LLM 做，人只审阅。

信息流：

```text
写散文 ──► 重读，选中一个说法 ──promote──► 概念（名字被监视）
                                              │ 系统主动：精确匹配
                                              ▼
                              候选提及（一次性读模型）
                                              │ 人：链接一处 / 节点内全部
                                              ▼
                     Org 里的链接、属性（文档事实）
                                              │ 数据库投影
                                              ▼
              backlink、stream、集合、表 ──► 促成下一次写作
                                              ▲
                              召唤 LLM：元数据候选、释义草稿、问答
```

## 4. 两个引擎

文本保存所有事实。数据库计算所有候选和视图，并随时能从文本重建。

- 数据库的作用是快速读取和发现。它是 grep、解析、连接三件事的缓存：grep 找到行，解析把行还原成节点，连接把节点关联起来。任何数据库能回答的问题，原则上都能用 grep 加逐个打开命中处回答，只是慢。反过来，凡是 grep 加阅读原则上答不出来的问题，数据库也不该回答，因为那意味着有事实不在文本里。
- 数据库里存的是指纹和投影，不是真相。现有 `supertag-services-sync.el` 中的 `supertag-node-hash` 已把 `:properties` 纳入节点哈希，属性抽屉改一个字，下次同步就会重建该节点。不需要比节点更细的变化检测。谁改的都一样：手改、LLM 写、git pull、sed，下次同步都以文本为准。
- 任何写入都必须经过活的 buffer 再保存，让 after-save 接住。禁止绕过 buffer 直接改文件，否则 buffer 里的旧内容会在下一次保存时覆盖磁盘。
- 序列化必须稳定。Org 属性的大小写、`PROP+` 累加、继承和空白都可能造成"没改却判成改了"。这里的风险是误报，不是漏报，稳定性比灵敏度重要。
- 全量 reindex 永远可用，是所有增量机制的兜底。
- 数据库没有写入命令。用户面对数据库只有三个动作：全量重建、重建当前文件、看状态。备份、恢复、冲突解决、锁、索引、手动保存加载，全部由重建取代，因为缓存丢了就再算一次。自动同步常开，不提供开关。冲突这个概念不存在：文本永远赢，数据库里不可能有文本没有的状态。所有修改状态的命令都写文本，数据库只在保存后跟着变。

## 5. 候选从哪里来

允许的来源：

| 来源 | 触发方式 | 例子 |
|---|---|---|
| 名字与别名的精确匹配 | 系统主动，这是唯一的主动行为 | Unlinked Mentions |
| 沿已确认链接算出的图结构 | 用户打开视图时 | backlink、共引、共现、标签传递 |
| 笔记库中的随机笔记 | 用户打开 discovery 时 | 打开即可阅读，之后按需筛选；随机呈现不意味着语义相关或值得优先阅读 |
| 相关笔记检索（允许语义检索） | 用户打开 node view 时，无须再执行查找命令 | 参考 Smart Connections，展示当前节点的相关笔记，供用户阅读、写作 |
| LLM | 用户召唤时 | 元数据抽取、给释义打草稿、回答问题、找相关笔记 |

不允许的：自动打标、未召唤的推荐、频次提示、健康信号、孤儿检查、未释义计数、未经用户召唤的后台 AI。打开 node view 是查看相关笔记的召唤；检索实现、索引构建和刷新策略尚未决定。

召唤时 LLM 可以做任何用户要求的事，包括找相关笔记，因为那是用户开的口。它不允许的只有一件事：主动替用户决策。

LLM 写元数据的两条护栏，都来自"错一条比漏一条更糟"：

1. 优先从节点正文里抽，不从 LLM 的世界知识里补。表里的错值是静默的。正文里没有、靠它自己知识补上的值，在审阅窗口里要看得出来。
2. 没有审阅不落盘。对属性的信任和对链接的信任是一回事。审阅窗口和 Unlinked Mentions 共用。

## 6. 不变量

1. 写的时候不需要任何结构。
2. 判断类状态由人亲手设；抄录类状态由 LLM 提出、人审阅。状态互相独立，系统不从一个状态推出另一个。
3. 每个状态都落在 Org 文本里，数据库随时可重建。
4. 系统主动显示你起过的名字出现在哪。其余一切等召唤，LLM 也是；打开 node view 即召唤相关笔记展示。系统对用户的习惯没有意见：不计债，不催，不给习惯打分。
5. 所有动作发生在光标处或选区上。

判断任何新功能的三条规则：

- 凡是替用户确认"这两样是不是一回事"的功能，不属于 Supertag。相关材料可以作为候选展示，不自动确认同一性或建立链接。
- 凡是让用户起过的名字更不容易丢的功能，属于 Supertag。它查的是功能有没有服务于记名。
- 文档事实必须能用 grep 加打开命中处复现。相关笔记候选必须能打开对应 Org 原文，但语义相关性及其排序属于检索计算，不是文档事实，不要求 grep 复现排序。这是 node view 相关笔记决定对原规则的收窄。

三条查三种病，过了一条不等于过了另两条。`grep -c` 能复现"这个词你用了三次"，这个提示仍然被不变量 4 挡在门外。

## 7. 删什么，留什么

删：

- Supertag 自己的 field 系统：schema、字段类型、字段定义与绑定、字段编辑 UI、字段校验。字段这个概念不删，Org 属性抽屉就是新字段：一个键一个值，没有类型，没有必填，谁都可以有。填写由召唤 LLM 提候选、人审核；人真要手改，Org 自带的 `C-c C-x p` 就够，Supertag 不提供界面。表格、看板、查询都读属性。
- 养。没有健康信号、孤儿检查、未释义计数。收件箱只展示，不催。
- 频次提示、未召唤的推荐。node view 中的相关笔记检索保留。
- 未召唤的 AI。召唤可以是执行命令，也可以是打开 node view 查看相关笔记；不因此授权写入。

留：写、promote、候选提及、链接一处或节点内全部提及、别名、标签、backlink、节点视图、stream、召唤 AI、Org 原生 TODO、Automation（规则人写，产出落 Org）。

封存：看板、Board、图视图、表格、embed 组件。代码保留，默认不接入下一版本；保留源码不代表默认加载或提供可用入口。

ontology 子系统不删，封存。它把知识和行动放在一起，标签接上 LLM 就是能执行行为的 AI Supertag，方向对，实现完整，只差接入模型；代码整体保留作参考，不接入主流程。现在从它借三条规则进模型：

- 凡是写超过光标处一个节点的操作（全库改标签名、迁移脚本、LLM 批量写属性），先给 diff，再执行。ontology 的 preview 把变更分成安全、行为、破坏三档，这个分档可以照搬。
- 用户确认的是精确的写入计划，不是意图。计划变了，确认作废。这是 ontology 的一次性确认令牌的精神，用在元数据候选审核上正合适。
- 召唤 LLM 时给它的是一张明示的操作清单，不在清单上的它调不到。ontology 的工具目录就是这个思路，将来接 LLM 时从那里起步。

## 8. 与现有文档的差异

`doc/OWNERSHIP-CONSTITUTION_cn.md` 把事实分成 Org 拥有的 Document Facts 和数据库拥有的 Semantic Facts。本模型把 Semantic Facts 缩到接近于零：

| 宪章中的 Semantic Fact                                      | 本模型的去向                                                                                                                                                                                                                                                                                                                                                                                           |
|-------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Semantic Tag 的稳定 ID、名称、alias                         | 标签退回正文里的 #tag，概念别名是 Org 属性。稳定 ID 是否保留见第 9 节                                                                                                                                                                                                                                                                                                                                |
| Tag schema、inheritance、field definition/association/value | 删除 field 系统，属性抽屉成为新字段。已有的值一次性迁成 Org 属性                                                                                                                                                                                                                                                                                                                                                                      |
| Semantic Edge | 改造，不删除。事实只写成 Org 带类型链接 [[关系名:ID]]；:relations 记录降为缓存，从链接解析、重解析时整体替换。属性键和值不派生关系；普通属性里的 id: 链接仍按普通链接读取。动作在 Org buffer 光标处，节点视图只显示。禁止任何只写数据库不写文本的建边入口。名字建议改掉，因为"Semantic Edge"在现有文档里就是"数据库拥有"的意思。见第 2 节"边" |
| Link Definition，即关系的源类型、目标类型、基数约束 | 删除。关系名是在用的词加可选配置的词表，不约束源和目标 |
| Board、Automation、持久 Query/View 定义                     | 未讨论，见第 9 节                                                                                                                                                                                                                                                                                                                                                                                      |
| 未解决的同步冲突 | 删除。文本永远赢，冲突不存在，supertag-conflicts 整个模块去掉 |

宪章的公式 `A = O ⊎ S`、`P = π(O, S)` 在本模型下趋近 `A = O`、`P = π(O)`。

## 9. 决定记录

- 已定（2026-09-05 link-only 修订）：用户明确纠正「关系只支持 link」。关系名只来自 Org 链接类型；属性键不派生关系。`:SUPPORTS: [[id:ID]]` 保持普通属性及普通 `id:` 链接含义，不生成 `supports` 边。本修订取代先前关于属性落点、两种写法统一、`KEY+` 关系派生及跨正文／属性关系名大小写的提议；它不决定 cold-start protocol 或旧配置迁移策略，也未实施生产改动。

- 已定（2026-09-05 修订 Embark 设计边界）：Embark 是原 supertag-act 所承担的可视化情景命令菜单入口，目的在于让用户发现并执行动作而不强记命令；具体设计留待后续按 WHERE（光标对象）→ WHAT（适用命令）→ ACTION（范围、结果及执行后位置是否符合预期）展开。一般以所在节点为对象，落在 #标签或具体 Org 链接时则以该对象为目标；不在节点内的情况单独设计，选区等边界未定。既有动作表降为能力盘点，不是最终对象—动作映射或菜单规格。“更换当前标签”可重新评估为情景动作，不恢复旧独立 change-tag-at-point 入口，也不预定新符号。不是必需依赖（但推荐已使用 embark 的用户配置）、原生 embark-act 入口与识别只读等方向保持；本轮仅修正文档，不实施菜单。

- 已定（2026-09-05 链接入口改名）：用户明确将 supertag-add-reference 改为 supertag-add-link。这是最终名称，替代此前 reference-insert／add-reference 命名；选区或光标处、已有或新建目标、普通链接及可选关系、属性落点的统一能力不变。旧 link-add/menu/remove 不恢复，其他 reference 命令不因本次命名决定自动改名。仅更新文档，源码尚未改名。后续 link-only 修订保留入口合并与命名，但取代其中“属性落点”表达关系的部分；本条保留当时决定原貌，不作为当前属性关系契约。

- 用户入口总览（2026-09-05）：用户授权整理最终用户入口清单，见 [COMMANDS-TRIAGE_cn.md 第 5 节](./COMMANDS-TRIAGE_cn.md#5-下一版本用户入口清单)。按日常入口、Embark 情景动作、视图局部操作、设置／库管理／Git 同步、缓存恢复分层呈现；覆盖原表全部“留／改”能力，不把封存、内部维护、一次性迁移混入日常命令。此为已确认决定的汇总，不是实现交付。

- 已定（2026-09-05 Node View 阅读预览）：相关笔记条目直接显示标题和一小段相关正文，用户不必逐个打开即可判断是否值得阅读，需要更多内容时再进入原笔记。用户明确同意，并提供下列五份参考。只确认展示需求，不因此引入新依赖或整套外部功能。
- 参考阅读（2026-09-05，外部项目仅作依据；末项预览行为已获后续确认，不据此采用项目全部功能或依赖）：
  - [Sacha Chua：org-db-v3 + Consult 预览](https://sachachua.com/blog/2025/10/playing-around-with-org-db-v3-and-consult-vector-search-of-my-blog-posts-with-previews/) 使用简短的年份/标题候选配合原文预览，并通过 Embark 操作多个链接。借鉴先判断再打开的交互，不把本文的 minibuffer 界面直接替换 Node View。
  - [Luis Moneda：Semantic Search for Org roam](https://lgmoneda.github.io/2023/04/08/semantic-search-for-org-roam.html) 讨论以节点内容检索、区分子节点内容，以及进一步分块并保留节点 ID/层级。借鉴结果回到原节点的能力；文章中的模型和依赖版本不是当前选型依据。
  - [org-roam-similarity](https://github.com/cpbotha/org-roam-similarity) 将相似节点接入既有 org-roam buffer，与 backlinks/reflinks 同处；借鉴既有视图内呈现，不新增另一套面板。其示例模型的中文适用性未验证，不直接照搬。
  - [vulpea](https://github.com/d12frosted/vulpea) 当前文档将其定位为 Org 笔记的数据库/查询底层，支持异步索引及外部文件变化检测。它不是相似笔记预览界面；本次没有决定替换 Supertag 底层或新增数据库。
  - [vulpea-ui](https://github.com/d12frosted/vulpea-ui) 提供上下文侧栏、引用和未链接提及；backlinks 可显示链接附近的正文。借鉴上下文预览，不引入其 schema health、统计提示或额外 collection 视图。
  - 已定（用户后续明确「同意你的建议」）：片段取相关原文而非统一正文开头；已有引用展示引用所在上下文，未链接提及展示名字或别名出现的上下文，语义结果尽可能展示相关段落。仅有节点级相似结果时，不假称某段为精确命中。打开原笔记时尽可能定位到相应位置；阅读和预览不插入引用，是否建链仍由用户主动决定，明确关联与候选性质始终可辨。实施时验证中文笔记的检索质量，不直接照搬参考项目的模型。此为已确认产品要求，尚未实现，也未安装或运行参考项目。

- 已定（2026-09-05 Node View 相关笔记顺序）：用户希望打开概念时，先看到已有引用和未链接提及，再看到语义相似但尚未建立关联的笔记。已有引用是已写入文本的明确关联；未链接提及只是名字或别名匹配的待确认候选，不因排列靠前而成为已确认关联；语义相似也只是检索候选。界面须保持这些性质可辨，不自动建链；具体布局及各类内部排序留待细化。本决定不增加独立查找入口或额外面板。

- 已定（2026-09-05 修订已有节点提升）：用户澄清复用已有节点也遵循 Promote：在对应概念文件里建立目标节点，原位置保留引用，并明确同意正文一起移入目标文件、原位置只留下引用。此前“不搬移”的约束仅适用于选区提升，不再笼统应用于已有节点提升。用户说明原先希望直接高亮概念词组，后来针对中文使用体验改为显式 Org 链接；已确认关联写成链接，unlinked mention 仍是待人工判断的候选。后续已确认：移动时沿用原 Org ID，让已有引用继续指向同一节点；没有 ID 才补齐。复用时保留已有正文和属性，添加模板标签，模板属性仅补缺失项，不覆盖已有值，不重复插入初始正文；全新节点才应用完整模板。本次仅更新设计，不执行笔记移动；实施时须验证 ID 连续性、已有引用可达性及模板非覆盖行为。

- 已定（2026-09-05 收尾确认）：Automation 初始化、索引与事件接入、Org 集成启停归内部生命周期；缓存清理、补全调试、数据库检查、概念刷新归自动处理或内部诊断。独立 complete-tag/reference-complete 入口退出，保留输入补全与统一添加入口；SVG 样式统一 supertag-toggle-tag-style。保留视图局部按键、对象适配、supertag-init 和必要 minor mode，Discovery 局部命名统一。删除入口不等于删除必要内部函数，也不机械移除局部按键所需 interactive。用户已对六组方案逐项表示同意。
- 已定（2026-09-05 同名消歧）：supertag-promote 遇到同名节点时，提供“复用已有节点”和“按模板新建”两个选择；选择视图必须显示同名节点的内容，帮助用户判断所指。用户说明这正是 unlinked mention 机制需要人工确认的原因；同名不是同一概念的证据。后续已确认：复用概念文件外的已有节点，不是仅链接它在原文件中的位置，而是将节点及正文移入模板目标文件，原位置只留下引用；由目标位置满足概念文件规则。具体预览布局留待实施。

- 已定（2026-09-05）：Node View 用户打开后跟随当前节点，关闭后停止；自动显示作为配置，不设独立日常跟随命令。用户对建议明确表示同意。本轮仅记录产品决定，未修改运行行为。

- 收尾审查（2026-09-05）：用户授权「同意，执行」后，已只读检查剩余初始化、集成、诊断、补全、视图按键及模式入口。批量建议与源码依据见 [COMMANDS-TRIAGE_cn.md 第 4 节](./COMMANDS-TRIAGE_cn.md#4-剩余入口收尾审查2026-09-05)。后续已确认删除 view-node-edit-at-point、将 resolve-data-directories 改为一次性迁移；分类计数已更新，其余批量收敛方案也已获用户逐项确认并回填分类，功能代码未修改。特别核实：view-node-edit-at-point 仅编辑旧字段；resolve-data-directories 是会重命名目录的迁移入口；Automation 两条事件通道有重复触发风险，不能一起自动启用。
- 已定：节点的 Embark 情景菜单提供“提取属性”。用户主动执行后，AI 从当前节点提出 Org 属性候选，用户逐条接受或跳过；接受后才经活 buffer 写入 PROPERTIES，跳过不写入、不产生整理债务。用户对这一入口及审核流程的原话：「我觉得是可以的。」三个旧 field 审核命令的能力据此改造，不沿用其旧名称；最终函数名与审核界面布局尚未决定。
- 已定：保留 SVG 标签显示，长标签必须完整显示所有标签文字，不因长度裁切或省略。用户原话：「保留，只不过现在显示比较长的 tag 时会截断，我建议完整显示 tag 字符」。这是显示要求，不改变 Org 中的标签内容。当前实现按字符宽度估算 SVG 宽度，截断原因尚未复现验证；后续验证应覆盖长英文、中文、混合文字及斜杠层级标签。后续已确认：启用/停用合为 supertag-toggle-tag-style，缓存刷新归内部处理。
- 已定：继续支持多个相互独立的笔记库，保留 supertag-vault-activate 切换入口与 supertag-vault-indicator-mode 当前库提示。用户原话：「继续支持多个相互独立的笔记库」。多库独立是下一版本保留的产品要求；具体隔离实现仍需在实施时验证。
- 已定：Git 同步继续由 Supertag 提供，保留 git-setup、git-clone、git-sync-now、git-sync-mode 四项能力。用户原话：「继续由 supertag 提供这套同步能力」。代码检查显示当前实现依赖数据库布局迁移、持久化和语义合并；根据已定文本事实模型，这四项标为改造，Git 同步 Org 文本、数据库作为本地可重建缓存。前文“冲突不存在”仅指文本与缓存间不存在两个权威版本，不代表多个设备修改 Org 文件不会产生 Git 冲突；后续已确认：进入已有可编辑冲突解决界面，暂停当前库自动 Git 同步，用户确认解决后继续 Git 流程并更新本地缓存；不自动选边、不另造合并器。具体工具接入留待实施。本轮未执行 Git 设置、克隆、同步或安装。
- 已定：保留“移除当前节点的标签”和“解除当前引用”两个情景动作，直接由 Embark 对对应对象提供。解除引用只去掉链接，保留原来的可见文字；移除标签仅影响当前节点，区别于全库移除。用户对两项建议的原话：「可以保留。」现有 supertag-remove-reference 会删除整个链接文本，须改造；此前删除旧 Link 移除入口的决定不等于取消这个文本编辑动作。（2026-09-06 改定：解除引用改为整体删除链接，见下条。）
- 已定：Embark 作为 Supertag 的必需依赖，安装 Supertag 时一并安装。用户原话：「embark 会作为依赖，在用户安装 supertag 的时候，同时也安装到用户的 emacs 库里」。情景操作统一通过 embark-act，直接展示适用于光标对象或选区的动作；不再保留无 Embark 的备用菜单。supertag-act、supertag-act-dwim、supertag-act-mode、supertag-embark-act、supertag-embark-act-dwim 退出独立用户入口，必要内部识别与动作适配保留。识别和展示菜单不修改笔记，执行需要身份的动作时才补齐 ID；已确定的直接命令仍可使用。本轮只确定产品依赖与入口，未修改打包配置或安装软件。
- 已定：查找统一使用 supertag-find-node，默认在当前窗口打开，带前缀参数时在另一窗口预览、打开；删除 supertag-find-node-other-window 独立入口。在 minibuffer 提示当前打开方式及另一种用法，帮助用户发现这项能力。用户原话：「我同意你的建议，这样子用户不必强行记住 2 个命令，只不过有多一个用法，可以在 minibuffer 处提醒用户」。
- 已定：discovery 保留选择多个节点、将其链接列表插回项目笔记的能力。用户场景原话：「如果我正在准备一些项目，希望快速地筛选出多个有价值的节点，可能会这么操作」。导出到已有文件和导出到新文件两个入口（supertag-search-export-results-to-file、supertag-search-export-results-to-new-file）暂时封存，代码保留，默认不接入下一版本。用户原话：「这两个命令可以暂时封存。」选择与插入命令的最终命名已确认统一使用 discovery 前缀，见命令清单第 4.2 节。
- 已定（2026-09-05 修订抽样边界）：stream 呈现符合条件的完整集合；discovery 打开时可随机呈现一部分供阅读，但用户主动筛选后必须显示全部符合条件的节点。用户原话：「沿用当前 search 的方法。以及筛选后应该显示所有符合筛选条件的节点。」筛选沿用空格分隔关键词、每词匹配标题/标签/正文/属性值之一的现有方式，不新增另一套筛选器。两者保留不同的阅读与处理体验，不再以筛选后完整集合与抽样区分；分页、加载与排序尚未决定。
- 体验澄清：用户原话：「stream 的假设是，用户完全按照 supertag 的预设，节点位置与最终信息呈现解耦。所以不管在节点在什么位置，只要具备对应的节点都能够直接通过 stream 筛选出来，显示其中。而且它的界面已经非常适合快速浏览和处理。」「discovery 提供的浏览体验和 stream 不一样。」「或许这里需要深度思考一下。」撤回仅凭筛选重合就把 stream 并入 discovery 的建议。当前代码的 stream 提供标签集合、按日期组织的标题列表、节点编辑及返回；search 提供内容片段卡片，discovery 的随机初始阅读尚未实现。后续决定已将 discovery 抽样限定为初始阅读，主动筛选须展示完整匹配集合。
- 已定：supertag-find-node 保留快速精准定位职责，找不到对应节点时可由用户主动选择新建。用户原话：「我一般用 find node，快速且精准定位位置（以及我认为这里，如果 find 里找不到对应的节点，可以新建 node）」。新建沿用 promote-concept 的目标文件机制，直接放在对应文件里，不另设 move-node 式位置选择；打开窗口遵循统一命令的前缀参数用法。这里创建节点不自动等同于 promote，也不恢复独立 create-node 命令。
- 已定：supertag-search 重定位并改名为 supertag-discovery，帮助用户重新浏览自己的笔记库、发现过往节点。用户原话：「但是 supertagg-search 的作用是，可以让我发现，过往的一些 node，它的存在具备一个【发现器】的作用，所以我觉得 supertag-search 可重新定位，它是帮助用户重新浏览自己的笔记库的，可重命名为 discovery。」与 stream 的呈现关系及搜索子命令去留继续讨论。
- 已定：discovery 打开即展示可阅读的随机笔记，不要求先输入查询；用户按需要添加条件筛选。用户原话：「discovery 打开就可读，可以显示 ramdom notes，用户根据自己的需要，再以一定的条件筛选」。随机呈现仅在用户打开入口后提供，不据此引入后台提醒或 AI 推荐；筛选沿用当前 search 且呈现所有匹配节点；初始随机展示数量和更新方式待细化。
- 已定：embed 组件整体封存，包括插入、链接转嵌入、刷新及内部同步与初始化接入；保留代码，默认不接入下一版本。用户原话：「是，这个对于很多用户重新组织文档时相当有用，但在 org-mode 这个场景里，它的体验不如 logseg 和 roam 的 embed 自然。以及，在 org-mode 的生态里，还有一个 package， https://github.com/nobiot/org-transclusion 实现了类似的功能。所以，我觉得这部分，可以【封存】。」这是用户对体验与生态的判断，参考项目为 [org-transclusion](https://github.com/nobiot/org-transclusion)。本决定不包含安装、集成该项目或迁移既有 embed 内容。
- 已定：保留 supertag-move-node 和 supertag-move-node-and-link。用户原话：「不， move-node 是非常有用的特性，它比 org-refile 更灵活，不需要预配置，就可以移动到任意位置。这个功能应当取代 org-refile。move-node-and-link 也有其价值。」产品要求是无须预配置即可选择目标文件和位置，作为用户工作流中替代 org-refile 的移动入口；后者移动后保留原处链接。不得仅因 Org 有移动能力就删掉这两个入口；此决定不意味着修改 Org 自身或全局覆盖 org-refile。选区 promote 不搬移所在标题；后续确认的已有节点提升会将节点及正文移入模板目标文件并留链，区别于 move-node 的任意目标位置选择。
- 已定：删除 supertag-capture-with-template 和 supertag-node-reference-and-create 两个独立入口。捕获统一使用 org-capture，引用目标创建由 supertag-add-link 承担；promote 模板仍保留。用户对两项删除建议的原话：「同意」。
- 已定：添加引用、标签等操作需要节点身份时自动补齐 Org ID，已有 ID 沿用；用户不需要先执行“创建节点”，删除 supertag-create-node 交互入口，保留内部身份处理能力。用户原话：「我希望是添加引用，标签等节点身份的时候，自动补齐。」普通写作不因此增加身份准备步骤。
- 已定：删除 reference-create-or-link、reference-link-region、ui-add-semantic-relation、ui-remove-semantic-relation 四个独立入口，引用与关系统一使用 supertag-add-link；标签改名最终统一叫 supertag-rename-tag，tag-rename 旧名称退出交互入口。用户对这批方案的原话：「同意，继续让我们讨论。」
- 已定：添加类命令统一使用 supertag-add-*。用户原话：「supertag-reference-insert 换成 supertag-add-reference」「supertag-insert-query-block 换成 supertag-add-query-block」。其中引用入口的旧命名已由后续 supertag-add-link 取代；添加类命名原则和引用能力沿用此前合并方案，替换旧 add-reference 实现，不恢复重复入口。
- 已定：查询入口最终命名为 supertag-add-query-block（原 supertag-insert-query-block），以 Org Babel 源码块承载查询正文；删除 supertag-insert-query-dblock 插入入口，已有动态块仍可读取和刷新。两种格式共用查询引擎，不需要两个用户插入入口。用户对格式合并方案的原话：「同意」。
- 已定：添加标签统一使用 supertag-add-tag，支持当前节点和选区内多个节点；删除 supertag-tag-insert 独立入口，保留直接输入 # 的补全。用户对合并方案的原话：「同意」。
- 已定：supertag-mention-service-clear-cache、supertag-scheduler-list-tasks、supertag-scheduler-start、supertag-scheduler-stop 四个命令退出用户交互入口；缓存由程序管理，调度器随功能自动启停，保留 Automation 所需调度能力和必要内部调试函数。用户对“去掉交互入口，保留必要内部能力”方案的原话：「同意」。
- 已定：删除 supertag-delete-node 和 supertag-back-to-heading 两个命令入口。删除内容使用 Org 原生子树编辑、保存后同步；不提供删除 ID 以撤销节点身份的专门命令。返回最初节点的导航需求仍然保留，与 back-to-heading 无关。用户原话：「同意，删除」。
- 已定：普通引用与带关系的引用职责统一，最终入口为 supertag-add-link；删除 supertag-link-add、supertag-link-menu、supertag-link-remove 三个独立入口。默认普通引用，关系名按需指定；属性抽屉是同一能力的落点变体，不要求普通引用多选一次关系。用户对能力合并方案的原话：「同意」。后续用户明确「关系只支持 link」，因此本条的入口统一仍有效，属性落点变体已被取代且不再表达关系。
- 已定：引用统一为一个入口，最终命名 supertag-add-link，承接 reference-insert 能力、替换旧 add-reference 实现，删除 add-reference-and-create 重复入口。有选区则替换选区，无选区则在光标处插入，支持已有目标与新建目标。早先用户原话「统一使用 supertag-reference-insert」所确定的能力保留，名称由后续 add-* 命名决定取代。
- 已定：保留 supertag-delete-tag-everywhere，全库移除指定标签而保留笔记本身；先预览影响范围和文本改动，确认后经活 buffer 执行。用户原话：「需要的，可以保留」。
- 已定：删除 supertag-change-tag-at-point 旧独立入口，当前标签仍可直接编辑 #tag；后续用户澄清“更换当前标签”可作为 Embark 情景动作评估，不能将删入口理解为禁止能力，最终行为与符号待设计。保留并改造全库标签改名能力，最终入口命名 supertag-rename-tag，全库改名先预览、确认后修改 Org 文本。此前用户对入口取舍的原话：「同意你的看法」。
- 已定：保留 supertag-mention-link-all-in-node。针对保留当前节点内同一概念全部提及的批量链接，用户原话：「我觉得可以保留。」删除 supertag-mention-ignore-in-node，用户原话：「supertag-mention-ignore-in-node 没啥必要」。不据此新增逐处忽略入口。
- 已定：用户使用 org-capture，删除独立 supertag-capture 和 supertag-capture-finalize-node-at-point 交互入口；必要的捕获收尾由内部集成自动完成。同时确认保留 supertag-mention-link，用于确认并链接一处候选提及。用户对三项收尾方案的原话：「可以」。
- 已定：打开 node view 时展示当前节点的相关笔记，无须再执行一次“查找相关笔记”。用户原话：「打开 node view 时出现」。参考 [Smart Plugins](https://smartconnections.app/smart-plugins/) 的写作辅助体验，允许检索不同措辞的相关材料；本决定修订此前全面排除语义相似检索的表述，保留由人确认链接、别名和概念同一性的边界。具体检索技术与刷新策略未定。
- 已定：查看参考材料后，能够直接回到最初的节点继续读写，并恢复离开时的位置。用户原话：「你的想法过度设计了，最关键的是要让用户及时回到一开始的 node 上」，随后确认直接返回起始节点的方案。后续已定：复用 Emacs 的标记与返回能力，不另造 Supertag 返回命令；实施时核实跨 buffer 与连续跳转的起点保存。不要为此引入固定面板、写作模式或双面板机制。
- 使用背景：用户主要回顾 diary 中最近写过的笔记，也会随意翻看，称 diary 相当于自己的 fleet note。打开概念后会读既有笔记、继续撰写，并希望得到类似 Smart Connections 系列的辅助；不据此把整套插件能力都列为已确认需求。
- 已定：promote 完成后应支持用户继续当前的阅读或写作，保持专注。用户原话：「我最近一次把某个说法提升为概念时，会希望继续专注在自己当前的活儿上，可能是继续阅读，可能是继续写。」交互推论（待细化）：不主动切到概念页或要求处理提及；选区提升应保持原文的阅读、输入连续性。
- 更正：用户原话：「标题不会挪走，只是在原地创建了一个 org-link，你可以查看 promote 的具体代码。」当前 [supertag-promote-concept](./supertag-concept.el) 接受选区，创建或复用概念，再经 [supertag-reference-materialize](./supertag-ui-reference.el) 在原地写链接，没有搬移标题的步骤；目标是源节点自身时不创建自链接。当前实现仍以 SUPERTAG_CONCEPT 属性标记概念，指定概念文件和模板是拟议模型，不能混同为现有实现。
- 已定：用户完成当前工作后，会主动找时间回顾笔记，再打开概念。用户原话：「我完成眼前的活儿了，我会找时间进入 review 模式，回顾自己的笔记，此时会想起，打开这个概念」。这里的 review 先指用户的活动，不据此推定需要新增软件模式、复习队列或定时提醒；具体入口和阅读过程继续讨论。
- 已定：收件箱在跟随光标的侧栏里，和 org-roam buffer 一样自动切换到光标所在节点。现有节点视图的 auto-show 机制就是它。全库范围只是同一侧栏换一个范围。
- 已定：拟议模型中，"已提升"的物理表示是位置。用户指定若干概念文件（默认 concepts.org），像 org-gtd 指定专用文件那样，凡是这些文件里的标题都是被监视的名字，别名是标题下的属性。选区 promote 在源处写链接、不搬移所在标题；复用文件外已有节点则将其连同正文移入模板目标文件，旧位置留下引用。grep 复现：grep 概念文件的标题行。后续已确认：出现同名节点时展示节点内容，由用户选择复用或新建，不按标题自动合并。后续已确认以节点及正文移入目标文件的方式完成提升，不将原文件（例如 diary）整体设为概念文件。
- 已定：标签没有稳定 ID，标签就是它的名字。改名是全库文本替换，先 diff 后执行。层级用斜杠路径 `#media/book`，从名字派生，不写数据库；数据库里的 `:extends` 继承删除。
- 已定：视图只留节点视图和 stream。看板、Board、图视图、表格封存，代码保留不接主流程。
- 已定：Automation 保留，不关闭；其中 rollup 汇总和字段间同步删除，汇总值读时算，不落盘。它是用户预先写下的规则，算提前召唤；产出必须经活 buffer 落进 Org 文本，不允许只写数据库。这是不变量 4 唯一的例外，例外的边界是"规则是人写的"。
- 已定：保存的查询只以 Org 文件里的 query block 形式存在，放在要看结果的地方。刚搬进 `:queries` 集合的代码倒回来。
- 已定（2026-09-05 细化）：Promote 模板由用户配置，包含名字、目标文件、初始标签，并可包含初始正文和 Org 属性；它们是创建内容的模板，不是强制 schema。统一入口 supertag-promote 提供类似 org-capture 的模板选择键；常用模板可定义 supertag-promote-person、supertag-promote-contact、supertag-promote-project 等直接命令并绑定快捷键，跳过模板选择。用户原话：「同意，而且，用户可以配置快捷键，就好像 org-capture 一样。」快捷命令共用同一提升逻辑，不默认增加一长串分类命令，也不要求每个模板都有独立命令。所有模板的目标文件都是概念文件，里面的标题都是被监视的名字；默认 concept 模板指向 concepts.org。Find Node 新建复用目标模板，但不执行源处替换为引用的 Promote 动作。Promote 创建或复用目标、原处生成引用，保持当前读写位置；初始内容不据此覆盖已有目标。具体配置变量、格式和快捷命令定义方式留待实施，不把示意配置当成现有 API。
- 实施：新开分支 `supertag-refactor`，整体改动在该分支上进行。
- 一次性迁移脚本：现有字段值写成 Org 属性。Semantic Edge 不需要迁移，当前数据库里一条都没有。
- 已定：抽取提示是一个配置变量，alist，形如 `(("book" . "作者、年份、出版社"))`，和 `org-todo-keywords` 同类。它是给 LLM 的一句话，不是 schema，不进 Org。
- 已定（2026-09-06 解除引用改定）：解除引用的命令改名为 supertag-delete-link，与 supertag-add-link 成对；执行时把整个 Org 链接（含描述文字）从源节点删除，不保留可见文字，随后保存并重投影。此条取代上文“解除引用只去掉链接，保留原来的可见文字”。用户原话：「删除 reference（应该重命名为 delete-link），应该直接全部删除，不要留下描绘文字。」Embark 情景动作仍按原计划在后续阶段提供。

以上是按话题列的。按代码列，批处理 Emacs 加载后共有 287 条 `supertag` 开头的交互命令，逐条对照本文档的归类见 [COMMANDS-TRIAGE_cn.md](./COMMANDS-TRIAGE_cn.md)：留 31，改 22，删 125，封存 93，一次性 16，未定 0。本轮剩余命令的批量收敛已确认；“留”含视图局部按键和配置模式，不等同于日常入口数。封存的是 ontology 子系统（理由见该文件 §2）和看板、Board、图视图、表格、虚拟列、embed 组件，以及两个搜索结果导出入口。

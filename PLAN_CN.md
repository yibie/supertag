# Supertag 重构计划（2026-09-06）

来源：`MODEL_CN.md`（模型与决定记录）、`COMMANDS-TRIAGE_cn.md`（命令去留）。本文件只写模块、顺序和已定决定；每阶段的证据写在工单回报里，不另建过程文件。

## 现状

分支 `supertag-refactor` 上用户入口已基本成形：promote 模板、add-link、add-tag、rename-tag、delete-tag-everywhere、find-node、move-node、mention-link、discovery、node view、stream 都存在。field/schema 系统仍是运行时骨架：node view、service-org、ops-tag、services-query、automation-sync、ops-relation 都在读写 field。field 值到 Org 属性的导出已有（`supertag-service-org.el:1647` `supertag-export-all-fields-to-properties`），缺预览与冲突规则；反向脚本在 migration.el。主线是把 field 从每个消费者里剥出去，再封存、再退役命令。

## 模块

| 模块 | 目标 | 现状 | 要做 |
|---|---|---|---|
| A 存储/投影 | DB 是文本投影；非重建事实、冲突、备份不被 reindex 删（M1/M3） | 三修已做（阶段 1）。缺口：reindex 删节点走 `supertag-node-delete` 级联删 field-values/relations，节点级事实的保护由 B 迁入文本后才成立；4f 已关闭（2026-09-06）：用户面 DB 命令只剩 sync-status / force-resync-current-file / sync-full-rescan，其余持久化/同步/恢复入口只退命令属性、函数保留（详见决定 14） | 用户面只留 sync-status / force-resync-current-file / full-rescan（4f 已做）；阶段 2 探针：列出 reindex 不重建的集合（sync-conflicts、automations、queries、views、meta）并断言 reindex 不清它们 |
| B 字段退出 | Org 属性即字段；版本门控迁移 | 6M 实现：旧 root/global/embedded 来源保留在 `:legacy-fields`，preview/apply 复用活 Org writer；五 field 集合/API/回滚分支/持久化旧守卫已退役 | 冲突与未知原值保留供审阅，先 apply 再孤儿清理；6M 已关闭（2026-09-07，决定 20） |
| C 标签 | 无 ID、斜杠路径、无 `:extends`、改名=全库替换先 diff | `:extends` 已删（4e）；仍有稳定 `tag-<uuid>` ID 与别名（ID=名 未排片）、tag-field 关联、schema-authority | B 后剥离；rename-tag 的后端 supertag-tag-rename 已不在工作树（HEAD 版只改 DB 名），3b 已完成（2026-09-06）：rename/delete-everywhere 改为预览缓冲（零写）→ 确认 → 逐节点 supertag-service-org-replace-tag / remove-tag（活 buffer、保存、重投影；失败即停，重跑续做）→ 无 owner 删实体；DB-先的 ops-delete-tag-everywhere 与跨文件文本 writer 已删；3c 已交付（2026-09-06，审查 0 必修）：svg-tag-mode-toggle 已改名 supertag-toggle-tag-style（enable/disable 内部化，无别名）；长标签截断已由用户 GUI 探针复现（est/real 恒 1.25），原因是宽度估算用行高系数 0.68 代替字号比，已改为 string-width × frame-char-width × (SVG 字号 / 默认字体像素字号)，batch 回退按行高；复测后仍裁 Latin，leader 用同套 librsvg 光栅化隔离：pango 在 macOS 默认走 CoreText 后端，Iosevka.ttc 超级 TTC 下家族名被选到 0.6 em 的 Extended 面（Emacs 自身 0.5 em），SVG 属性与 Emacs 侧测量均无法对齐，只有 PANGOCAIRO_BACKEND=fc（进程启动前）才回 Regular；3c-2 已交付（审查 0 必修、收尾完成）：新 defcustom supertag-svg-tag-min-column-em 默认 0.6（每列宽度下限，nil 关闭）与 supertag-svg-tag-font-family（nil=默认 face 家族），估算取 max(精确估算, 列数×字号×0.6)，缓存键加 family/min-column-em/padding-x/font-weight；真实 librsvg 光栅化五样本均不裁；用户 GUI 复测六样本全部完整，3c 已关闭（2026-09-06）；4e 已关闭（2026-09-06）：层级只由标签名的斜杠路径推导，`:extends` 退役，详见决定 14；模块 C 待办：ID=名、显式斜杠 `:id` 与名不等的兼容政策、无实体父路径的查询域统一（4e S1/S3） |
| D 概念/提升 | 模板目标文件即概念文件；M4 retry 安全；M6 监视资格 | 模板、目标文件、复用搬移已有 | M4 探针；快捷命令定义方式；find-node 新建走模板 |
| E 链接/关系 | 只有 `[[id:]]` 与 `[[关系:ID]]`；关系名只来自链接类型 | 2c 已做：链接特性并成 `supertag-link.el`（引用查询/命名链接查询/写入恢复/补全/命令/视图段六节），关系类型注册成 services-sync 的"关系类型"节，ui-link/view-link 封存于 `archive/link/`；旧入口（add-reference、reference-insert、reference-complete、link-add/remove/menu）已退出。ops-relation 仍有 field-reference 和 from/to-property 配置 | 阶段 3：删 field-reference 死代码与 notion-style/database-relation 配置；remove-reference 改名 delete-link 并整体删除链接（MODEL 2026-09-06 改定）；link-definitions 按决定 13 方案 A |
| F 提及 | 精确匹配监视名；链接一处/节点内全部 | 2c 已做：并成 `supertag-mention.el`（候选扫描/命令/视图段三节）；清缓存与节点范围忽略入口已退出 | 阶段 3：提及段只对 supertag-concept-node-p 目标出现（别名来源已一致：:SUPERTAG_ALIASES） |
| G 节点视图 | 属性、引用→未链接提及→语义候选，带上下文预览；自动跟随 | 5G-1 已关闭（决定 17）；5G-2a 的 timeout/Stop/Retry/上下文调和与 5G-2b 的段落预览/定位已通过独立复审 | 语义卡只推荐、预览和跳转，不提供 [Add link]；5G-2c 按用户决定撤回（决定 25）。双语配置说明、真实库质量校准仍待，5G-2 尚未总关闭 |
| H Stream/Discovery | stream 完整集合；discovery 随机→筛选显示全部，多选插回 | 都已实现，有测试；两个 export 入口封存于 archive/discovery/（显式 load 可用）；search- 前缀全部改 discovery-，特性文件 supertag-discovery.el | 4c 已关闭（2026-09-06）；A-DAY 旧 supertag-search 示例留文档阶段 |
| I 查询块 | 只有 Org query block | add-query-block 已改名；`:queries` 已倒回（加载时丢弃）；保存查询登记已删；query-library 并入 supertag-query-block.el；谓词与自动表头已读属性（2b）；query-build/describe-syntax 保留（用户 2026-09-06：降低写 query 表达式难度） | 4b 已关闭（2026-09-06）；`:embeds` 写回与 README/QUERY.md 旧模型文案另议 |
| J Automation | 规则在配置、产出经 buffer 落 Org、无 rollup/字段同步 | 2b 已做：触发/条件/动作读属性；只剩一条 Store 订阅且 init 幂等；after-operation-hook 通道已无 Automation | scheduler 三条入口已退（4a，2026-09-06，内部启动保留）；持久化规则里的 field 引用由迁移预览列出，用户手改（决定 9） |
| K Git 同步 | Org 文本传输，本地投影重建 | Git 与 DB 语义合并混合 | 6K 已关闭（2026-09-07，决定 19）：只跟踪 Org/根 ignore，smerge 暂停与续做，DB 合并封存（决定 19） |
| L 多库 | 相互独立 | vault-activate/indicator 在 supertag.el | 6L 已关闭（2026-09-07，决定 21）：切换前保存与守卫、运行时状态清理、按库路径与 Node View 空态均已锁定 |
| M Embark | 可选依赖（推荐）；情景动作统一 embark-act | 所在节点、选区与投影视图对象共用 target/keymap | 5M-3 已关闭（2026-09-07，决定 23）；Embark 可选，不进 Package-Requires |
| N AI 召唤 | 提取属性候选逐条审核；提示词 alist | 仓内无 LLM 客户端；支持取消、按标签串行提取与精确计划预览 | 5N-1、5N-2 已关闭（决定 16、24；5N-2 于 2026-09-07 收口）；superchat 可选；真实模型调参与其它召唤仍留待 |
| O 封存/退役 | 看板/Board/表格/图/embed/ontology/虚拟列/仪表视图封存；命令退役 | 一次性迁移入口收缩到 `supertag-migrate.el` 四命令；三个旧迁移文件已移入 `archive/migration/` | 6O-1、6O-2 均已关闭（2026-09-07，决定 22，复审五「可关闭」）；view-framework/helper 为当前横切能力保留 |

## 顺序

每阶段默认测试套件绿再进下一阶段。

1. O 封存移出加载链 + A 存储三修。
2. B 字段迁移与消费者切换；顺带 G 元数据段、I 谓词、J 触发、C/E 的 field 剥离。首批特性合并：link（services-reference、ui-reference、view-reference、services-link、ui-link、view-link、services-text-link → `supertag-link.el`）和 mention（services-mention、ui-mention、view-mention → `supertag-mention.el`）。2c 探针后调整见决定 12。已完成（2026-09-06：2a/2b/2c 均经 codex_support 审查、补修、独立复核：默认 16 套件 279/279、archive 5/5、guidance PASS、全源加载 OK）。
3. D/E/F/C 打磨：M4 探针、delete-link、rename diff、svg 宽度。拆分与探针结论见决定 13。
4. 改名与退役：add-query-block、sync-full-rescan、toggle-tag-style、H 的 export 封存、125 条命令逐条退役。 4a–4f 已关闭（2026-09-06）：退役集合仍在加载链的 8 条命令均已归属——conflicts-resolve/use-ours-all/use-theirs-all→阶段 6 K，act/act-dwim/act-mode/embark-act/embark-act-dwim→阶段 5 M；封存 13 条→阶段 6 O，一次性 11 条→阶段 6。阶段 4 关闭；阶段 6 O-1/O-2 已关闭（2026-09-07，决定 22）。
5. 新能力：M Embark、N AI 提取、G 语义层。 5N-1 已关闭（2026-09-06）。 5G-1 已关闭（2026-09-07）。
6. 已关闭（2026-09-07：K 决定 19、M 决定 20、L 决定 21、O 决定 22，文档与测试清单更新随决定 22 完成）。K Git 收缩、L 多库验证、文档与测试清单更新；一次性迁移收缩为版本门控迁移（决定 20）。DB-only 自动跑；字段与 `:extends` 路径改名须预览确认后复用既有 Org writer；先 apply 再孤儿标签清理。

## 已定决定（2026-09-06）

1. 封存形式：文件移入 `archive/<子系统>/`，不在 load-path；`archive` 测试套件单独加载。
2. 迁移映射：属性键沿用字段名原样，值取显示字符串，多值空格分隔，类型信息丢弃（细化见 7）。
3. progress-dashboard、effort-distribution、priority-matrix 一并封存。
4. AI 后端：用户自己的 superchat（`/Users/chenyibin/Documents/emacs/package/superchat`）。
5. 语义候选层：参考 chai（`org-zettel-ref-mode/chai` 的 chai-vector / chai-index / chai-search）的经验：按节点而非段落嵌入，向量只补候选、词法命中才是权威，CJK 分词单独处理。
6. Automation 触发：保留 Store 事件，不加保存后文本 diff 通道；只留一条订阅。
7. 迁移映射细化（2026-09-06 确认）：键 = 字段名大写，空白与冒号转 `_`，其余字符（含中文）保留；节点引用值写 `[[id:ID][标题]]`，多值空格分隔；Org 已有不同值时保留 Org 值，预览列出冲突；预览只列不写，apply 经活 buffer 保存并重投影。
8. 五个 field 集合与 core-store 的读写器阶段 2 保留（迁移脚本要读），阶段 6 跑完脚本后连同持久化的 field-values 守卫一起删；同批删除的还有 core-transform 对 `:fields`/`:field-values`/`:field-provenance` 的事务回滚分支与 conflicts 的字段冲突解决分支（它们是这五个集合的基础设施，阶段 2b-2 明确保留）。 已完成实现（6M，待独立复核）：读取原始根键提取待迁移来源后退役上述五集合基础设施；conflicts 已于 6K 封存。迁移保留为版本门控特性，不以删除原始来源代替导出。
9. 持久化的 automation 规则含 field 触发或动作：预览只列出，不自动转换，用户手改。
10. link-definitions 留到阶段 3（E）；阶段 2 只删 ui-link-definition 与 view-schema 的链接定义 UI。core-schema 的关系类型注册随 `supertag-ui-add/remove-semantic-relation` 一起删，关系名只来自 `supertag-text-link-relation-types`；core-schema 的时间校验迁入 core-persistence。
11. 阶段 2 分三张工单顺序执行：2a 迁移脚本（新文件 `supertag-migrate-fields.el`，导出代码从 service-org 搬出），2b 消费者切换与卸载六模块，2c link/mention 特性合并。
12. 2c 合并的三处调整：services-text-link 的关系类型注册被 services-sync 的 `[[关系:ID]]` 解析依赖（:995/:1608/:2146），并入 services-sync 作"关系类型"节，不进 link 特性文件，避免跨切文件 require 特性文件；ui-link、view-link 的三个入口（link-add/link-remove/link-menu）按 TRIAGE 皆删且外部零引用，两文件整体移入 `archive/link/`；ui-commands 里的链接命令与 helper（add-reference、remove-reference、document-link-bounds、replace-region-with-reference）迁入 `supertag-link.el`，消除 ui-commands 与 ui-reference 之间的函数体 require 循环。
13. 阶段 3 探针结论与切片（2026-09-06）：M4 成立（Promote 完成只由 state 与文本快照决定，retry 不查关系、不凭节点 ID 推断；promote 套件 35 例）、find-node 新建已走模板，二者不开工单。切片顺序 3a 链接/提及/概念小改（delete-link 整体删除；提及段只对概念节点；新增 supertag-define-promote-command 宏定义快捷命令）→ 3b 标签改名/全库删除走 Org 先加预览 → 3c SVG 长标签（需用户在图形 Emacs 复现后再改）→ 3d 关系层清理：删 ops-relation 的 notion-style/database-relation 与 :field-reference 文案；link-definitions 取方案 A：删 ops-link-definition、ops-relation 的 supertag-link-* 家族、api-link、view-api 条目、ops-tag assert-deletable，查询算子 link/reverse-link/has-link/has-reverse-link 改按关系名过滤 :document-link 关系，core-store 的 :link-definitions 集合按决定 8 留到阶段 6。 3a、3b 已关闭（2026-09-06；均经 codex_support 审查与复审，默认 17 套件 298/298、guidance PASS、全源加载 OK）。3b 附带决定（leader 定，用户可否决）：改名的新名若解析到另一现有标签的名或别名，预览与确认明示「并入已有标签 X（token 规范名）」后合并，不报错——同名即报错会阻断失败后重跑续做；新名是本标签自身名/别名则拒绝；确认后写前复核目标 id 与规范 token 均未变，变了则拒绝零写。 3d 已关闭（2026-09-06；审查 0 必修，非阻塞 S2/S3/S4 已收尾，默认 18 套件 304/304、guidance PASS、全源加载 OK）：ops-link-definition 封存到 archive/relations/，typed-link 实例族、notion-style/database-relation、api-link 写入口、view-api 定义分支、ops-tag 删除校验均已删；查询算子 link/exists-link/reverse-link/has-link/has-reverse-link 改按关系名查命名 Org 链接；已知变化：底层 supertag-relation-create 不再拒绝 :sync-field/:rollup 类型（根生产代码已无构造入口）。继承缺口（审查 S1，非本片）：supertag-ops-tag-merge.el:328/713 调 supertag-generate-relation-id 漏第 7 参 relation-name，整表重建会丢命名/普通边之一，重新接入或迁移 tag-merge 前必须先补并用同端点普通+两种命名边锁定。 3c 已交付并经审查（0 必修）后用户复测仍裁 Latin；3c-2（2026-09-06，审查 0 必修，非阻塞 S1/S2/S3 已收尾，默认 19 套件 314/314、guidance PASS、全源加载 OK）以代码侧有界兜底解决：机制是 librsvg→pango CoreText 后端把 Iosevka 超级 TTC 的 Extended 面（0.6 em）当作家族默认面，Emacs 自身度量 0.5 em，任何 SVG 属性（font-stretch/weight/style、Fixed/Term 家族、textLength）与 Emacs 侧测量（含 string-pixel-width）都无法对齐；新增 supertag-svg-tag-min-column-em（默认 0.6，nil 关闭）与 supertag-svg-tag-font-family，缓存键补齐 family/min-column-em/padding-x/font-weight，svg-tag 测试补 font-info nil/非正分支（原非阻塞待办已清）。已知代价：所有胶囊比精确值宽约 20%（CJK 按 2 列 = 1.2 em）；每列宽于 0.6 em 的字体靠 padding 吸收，极长标签仍可能裁，docstring 已写明。环境侧替代（用户自选）：以 PANGOCAIRO_BACKEND=fc 启动 Emacs 后可把下限设 nil。用户 GUI 复测（Iosevka 14px，est 36/288/216/188/58/58）六个胶囊全部完整，3c 已关闭（2026-09-06）。阶段 3 全部切片（3a/3b/3c/3d）关闭，阶段 3 收官。
14. 阶段 4 探针结论与切片（2026-09-06）：对照主仓加载链现有 138 条命令与 COMMANDS-TRIAGE 表，退役集合仍在的有 删 60、封存 13（view-framework/helper，随 O 阶段 6 处理）、改 1（insert-query-block）、一次性 12（阶段 6）；逐条消费者表在 leader scratchpad。机制/惯例区分：(1) mention-service-clear-cache 与两个 search-export 已非命令（discovery 套件断言 commandp 为 nil），不再开工；(2) 生命周期/诊断入口 automation-init/cleanup/rebuild-rule-index、automation-sync enable/disable/toggle-async、scheduler start/stop/list-tasks、org-capture 与 org-id 两对 enable/disable、completion-debug/db-inspect-file/svg-tag--clear-cache/concept-refresh/view-node-toggle-auto-show 都有加载期或钩子内部调用者（automation:1062、core-index:215、supertag.el:551、services-capture:849、service-org:899、svg-tag:296、concept:246），只去 interactive/autoload cookie/菜单，函数保留；测试无 commandp 断言，只以函数调用或 cl-letf 打桩引用；(3) `:queries` 消费者：core-store:57 集合表、query-library 的 query-save/run-saved/insert-saved 与 saved-map-forms、ops-tag:484 函数体 require（删标签前查已存查询引用）、默认套件 storage-save-boundary:128 与 document-query-compat:14 把它列为须保留集合、菜单 70-72/204-212/366-368；`#+BEGIN: supertag-query` 的 org-dblock-write 是持久化消费者，保留渲染只退 insert-query-dblock 入口（triage 已定）；query-build/describe-syntax 不在 triage 表（后加），保留，builder 的保存分支随 `:queries` 倒回；(4) H：supertag-search-* 剩 30 个内部符号，defcustom supertag-search-history-file 是配置消费者，默认套件 discovery-workflow-test 直接引用 4 个 search- 符号须同步改；export 三函数无 dblock 写函数，`#+BEGIN: supertag-search-export` 只是静态块头，非默认 tests/supertag-link-writer-boundary-test 只列不改；(5) 持久化/同步 删 项按 M1 与 triage §1 纠偏只退入口、函数保留：有内部调用者的 accept-fresh-store/load-store/save-store/index-clear-all/index-rebuild-all/reindex-org/restore/backup-database-now/db-retry-lock/doctor/relation-cleanup-duplicates/sync-start|stop-auto-sync/sync-force-resync-file/sync-check-now（git.el 4 处，K 改造）去 interactive 与菜单；0 调用者的 start/stop-auto-sync 包装、sync-reset-state、sync-full-initialize（sync-full-rescan 已是终名）、update-node-at-point 整体删除；async-retry-failed 是 core-async:128 失败提示指向的唯一恢复入口，退役时提示改指 sync-full-rescan，并由其清空 failed 列表（工单内先探针）；conflicts-resolve/use-ours-all/use-theirs-all 留给阶段 6 K（triage §4.3 决定 5 接入冲突界面）；migrate-reciprocal-links 三条只摘 supertag.el:464-471 autoload 与菜单 122/333，migration.el 归一次性；(6) act/act-dwim/act-mode 与 embark-act 两条归阶段 5 M。切片顺序：4a 生命周期/诊断入口内化（第 2 点 19 条 + 菜单 109-114/279-297/392-396）→ 4b 查询块（insert-query-block 改名 add-query-block；退 insert-query-dblock 入口保留 dblock 渲染；倒回 `:queries`：core-store:57、query-library 保存三命令与 saved-map-forms、ops-tag:26/484、两默认测试、菜单）→ 4c H 封存（export 三函数移入 archive/discovery/supertag-discovery-export.el；search-* 内部符号改 discovery-*，defcustom 改名不留别名，默认套件同步）→ 4d 重复入口退役（complete-tag、tag-insert、create-node、delete-node、back-to-heading、change-tag-at-point、node-reference-and-create 保函数去入口、set-tag-parent/clear-parent、view-node-edit-at-point 若仍在）→ 4e `:extends` 删除（ops-tag 90-163/299-325/446/513-516/598-606、ops-tag-merge 181-670、api:192/198、migration-preflight:110/178；migration.el 不动）→ 4f 持久化/同步入口退役（第 5 点）。每片 codex-support 只读审查；非默认测试只列不改；每条退役在工单回报记消费者证据。leader 判断供否决：restore/backup/doctor 退役后无用户恢复入口（§5 目标清单无此项，MODEL_CN 把 DB 当可重建投影）；search-history-file defcustom 改名不留别名。 4a 已关闭（2026-09-06）：18 个函数只去 interactive（completion-debug/concept-refresh 另去 autoload cookie），菜单 declare/包装/条目五组成对删除；审查 1 必修（README 把 M-x 改成日常求值仍是维护步骤）已按整句删除收尾，0 非阻塞；快照逐顶层 form 比对只差 18 处 interactive；默认 19 套件 314/314、guidance PASS、菜单与全源加载 OK。工单原写 19 是把已非命令的 mention-service-clear-cache 计入，实为 18。 4b 已关闭（2026-09-06）：insert-query-block 改名 add-query-block 不留别名，insert-query-dblock 入口删除而 dblock writer/Babel 执行器保留；`:queries` 只在加载侧退役——core-persistence canonicalize-store-root 对字面 `:queries` 根 remhash（写函数 write-canonical-store 遍历整个 Store 不筛声明，故仅删声明不会停止落盘；不泛化过滤未声明根），core-store 删声明，storage-save-boundary-test 新增真实 save→load→save 回归（13 集合非空哨兵，link-definitions 与 ontology-* 共 8 个部署/schema 集合无当前加载链生产者，只验空计数）；保存查询登记（query-save/run-saved/insert-saved/saved-map-forms/saved-list、defcustom query-saved、legacy 导入）整体删除，不导出旧 `:queries` 条目；query-library.el 并入特性文件 supertag-query-block.el（mv 原 ui-query-block.el），query-build/describe-syntax 保留（用户：降低写 query 表达式难度）；ops-tag 引用扫描不再依赖保存查询。审查 1 必修（函数体内 require org-table 改为唯一顶层 require）＋4 非阻塞（死 helper 删除、内部符号统一 query-block--、REF 帮助与 ABOUT-QUERY-BLOCK 兼容说明校准、回归测试加非空哨兵）均已收尾；默认 19 套件 315/315、contract 20/20、guidance PASS、菜单与全源加载 OK。未处理：`:embeds` 写回行为保留（docstring 已纠正，归档组件另议）；README/doc/QUERY.md 的 fields/typed-Link 旧模型文案留文档阶段；ops-tag-merge.el:21 等与 migration.el 的 legacy query-saved 引用留 4e/阶段 6；非默认 query-library-test/query-block-test 等只列不改。 4c 已关闭（2026-09-06）：export 四个 defun（get-selected-nodes、export-results-to-new-file、export-results-to-file 及只被其调用的 helper --insert-generated-node-link-line）逐字节剪入 archive/discovery/supertag-discovery-export.el（require supertag-discovery，默认不加载，显式 load 后可用；未新增 dblock writer，`#+BEGIN: supertag-search-export` 仍是静态块头）；mv supertag-ui-search.el → supertag-discovery.el 并加 Commands/Dependencies 头；余下 search-* 全部改 discovery-*（defcustom history-max-items/history-file、defgroup、四 defface、helper 与 find-nodes/highlight-current/get-history），不留 defalias/defvaralias，history 默认文件名 search-history.el 不变以续读已有历史；默认 discovery-workflow-test 同步，export 断言由 commandp 改 fboundp（封存后默认链不定义它们，commandp 对未绑定符号恒 nil）并加 find-nodes/history-file 旧名否定断言；A-DAY 双语删 export 表行与段落。审查 0 必修 0 非阻塞（封存件与快照逐字节一致，机械改名比对只差文件头/docstring/四 defun 删除）；默认 19 套件 315/315（discovery 24/24）、guidance PASS、全源与菜单加载 OK、mapatoms 无 supertag-search 前缀存活符号。未处理：A-DAY 旧 `M-x supertag-search` 示例留文档阶段；非默认 view-runtime/perf-benchmark/test-ui-search-performance/link-writer-boundary/archived-view-entrypoints 旧引用只列不改；封存函数保留历史实现（to-file 仍是未实现提示）。 4d 已关闭（2026-09-06）：九个重复入口退役——ui-completion 的 supertag-complete-tag、supertag-tag-insert 连 autoload cookie 整体删除（`#` 补全 CAPF 与 --get-prefix-bounds 保留，现 6 处调用者；:487 提示改指 supertag-add-tag），ui-commands 的 create-node/delete-node/back-to-heading/change-tag-at-point/set-tag-parent/clear-parent 六个 defun 整体删除（view-node-edit-at-point 生产链本已无定义），services-ui:545 node-reference-and-create 只去 `(interactive)`（:521 内部调用保留），菜单 change-tag 的 declare/包装/条目成对删除；services-sync:289 auto-create-node docstring 改指 add-tag/add-link，依据是两者投影前都经 --get-containing-node-at-point→supertag-node-identity-ensure-at-point 先把 ID 写进活 Org 文本（add-tag ui-commands:329→:95/:101/:105；add-link link.el:1016→materialize:555→source-id-at-marker:537），边界是 ID 进入活文本而非 save-buffer；默认测试 sync-worker-regression:284-295 与 node-identity:206 改调 supertag-service-org-create-node-at-point（被删 create-node 的标题分支只是调用该服务后 message，fixture 都在标题行，断言未改）。审查 0 必修 0 非阻塞（Emacs reader 逐顶层 form 比对：completion 37→35、ui-commands 67→61、services-ui 49→49 只少 interactive）；默认 19 套件 315/315、guidance PASS、全源与菜单加载 OK。未处理：service-org-delete-node-at-point:1348、demote-node-at-point:1366 删后 0 生产调用者，横切层保留，留阶段 6；`supertag--clear-parent`（0 调用者）与 ui-commands 继承模型注释块随 4e；update-node-at-point 留 4f；ui-commands.el 旧头无 Commands/Dependencies，重组留阶段 6；doc/architecture/02:38/48/60 为分析记录不改；非默认 tag-membership-org-first:281、test-ui-act:210/233、legacy-entry-retirement:176、test-back-to-heading、document-command-ownership 旧引用只列不改。 4e 已关闭（2026-09-06）：`:extends` 退役，层级只由标签名的斜杠路径推导——显示路径=清洗后的标签名（未知 id 原样返回），后代关系=supertag-tag-path-descendant-p（父不必存在为实体），tag-create 遇 `:extends` 直接 user-error，斜杠 id 守卫改为路径合法性检查（空段报错）；ops-tag 删 --normalize-tag-extends、--path-for-name 与 --referenced-ids 的 tags 遍历，ops-tag-merge 删继承冲突与继承改写（--rewrite-inheritance-and-delete-sources→--delete-sources），api schema、services-ui 斜杠分支、services-sync normalize-tag-id（解析 id 否则清洗路径）、services-query tag-children（按显示路径的父）、ui-completion 补全表与 post-completion（新名=safe-prefix，不再造父别名与父冲突）同步；已持久化的 `:extends` 惰性保留，加载时不剥离，阶段 6 一次性迁移用 3b 改名流把子标签改成显示路径后再删键（migration-preflight:105-111 保留做盘点）；行为变化：只被 `:extends` 引用的标签现在算孤儿候选。审查 1 必修 4 非阻塞：R1 是 leader 工单误写 `:conflicts nil` 丢掉 merge plan 的文件预检（:75-91），已恢复 `(supertag-tag-merge--file-conflicts nodes)` 并新增第 21 默认套件 tag-merge-plan（missing-file/unsaved-buffer 两例：计划报冲突、execute 拒绝、标签与磁盘不变）；S4 整理已做（ops-tag 长行/空行/冗余绑定、merge 空行、services-ui 空行、ui-completion U+200B 转义、core-scan:20 文案）。默认 21 套件 323/323、guidance PASS、全源+菜单+显式 merge+require migration 加载 OK。留待：S1 无实体父路径的查询域不一致（ops-tag:169-181 descendants 索引只为已有父实体建、:323-326 未知 key 返 nil，services-query:151-160 却接受未知父路径）归模块 C 统一；S2 见阶段 6 前置（迁移先于孤儿清理，否则 preflight 无法恢复父名）；S3 显式带斜杠的 `:id` 与 name 不等的兼容政策与 ID=名 一起归模块 C（merge:415 仍可能是新稳定 ID+目标名，不可概括为 id/name 总相等）。非默认 tag-merge-test:25/58/81/270-271/321/518-519 旧父 API、继承环、unsaved 冲突用例只列不改。 4f 已关闭（2026-09-06，分 4f-1/4f-2 两片，各经 codex-support 只读审查：4f-1 必修 0/非阻塞 1（sync-worker-regression 补断言，已做）；4f-2 必修 0/非阻塞 1（README 双语降级括号补"随后立即退出 Emacs 再用旧版重开"，已做）；leader 通读 4f-2 diff 查出主菜单 ("l" "Add link" supertag-menu--add-link) 被按键批量误删，已按快照逐字节恢复）：4f-1 持久化/同步入口——supertag-sync-full-rescan 成为唯一公开重建命令（services-sync 真命令，调内部 supertag-reindex-org，报告块搬入命令，complete 后经新 supertag-async-clear-failed 清空失败重试列表），reindex-org/start-auto-sync/stop-auto-sync/sync-check-now（搬入 services-sync 非交互，git.el declare+funcall）/backup-database-now/save-store（删恒假交互分支，supertag--persistence-refuse-save 现 0 调用者保留）/load-store/index-clear-all/index-rebuild-all/relation-cleanup-duplicates（0 调用者保留）去 interactive；ui-commands 删 update-node-at-point/sync-full-initialize/sync-reset-state/start-auto-sync/stop-auto-sync/sync-check-now 六个 0 调用者包装，sync-force-resync-file 改 FILE 必填非交互（force-resync-current-file 唯一调用者）；async-retry-failed 删、失败提示改指 full-rescan；supertag.el 摘三个 reciprocal-migration autoload（migration.el 显式 require 仍可用）；菜单 y/mr/mx 删、x=Full rescan；setup/git/doctor 提示与 README 双语、SYNC-CONFIGURATION（开关命令段改重启指导）、A-DAY 双语、OWNERSHIP-CONSTITUTION 同步。4f-2 恢复簇（用户决定：整簇按 M1 一起退、不推迟、含 resolve-data-directories）——doctor/restore/accept-fresh-store/db-retry-lock/resolve-data-directories 只退命令属性（cookie、interactive、菜单 l/d/R 三组 declare/包装/条目），函数保留可 Lisp 调用：supertag.el doctor autoload 第 4 参改 nil（不 require 直接 (supertag-doctor t) 仍自动加载且 commandp=nil，已断言）；resolve-data-directories 去 called-interactively-p 守卫、比较面板无条件显示；doctor 修复流 call-interactively 改直接调用/funcall（五个修复函数均零参 (interactive)，等价），修复清单 reindex-org 项改 sync-full-rescan，无引用的 reindex-org declare 删；运行时提示与文档 `M-x supertag-X` 改 `M-: (supertag-X)`/"evaluate (supertag-X)"，锁冲突提示改"重启 Emacs"；README 双语删入门第 3 条与排障 doctor 行，降级说明改为 Emacs 关闭后手工复制 premigrate 快照（persistence:2029 该快照是原库 copy-file）；MIGRATING:67 同步；conflicts.el:495 提示改"下次自动保存重试"。默认 21 套件 323/323（sync-worker-regression 现断言 aborted/failed 保留、complete 清空、三次返回 eq 原报告、活动队列不变）、guidance PASS、全源/菜单加载与 check-declare（git/doctor 转 nil）OK。留待：doctor 退役后 0 生产调用者（决定 14 第 (5) 点"有内部调用者"对它不成立）、supertag--persistence-refuse-save、relation-cleanup-duplicates 去留归阶段 6；非默认 persistence-hardening-test:168（旧 M-x 提示串）/:788（call-interactively save-store）、git-sync-mode-test:119-128（要求 doctor commandp）/:964/:1017、text-link-node-view-test:121、tests/supertag-link-test:166、reciprocal-migration-test:90、conflicts-test、supertag-restore-test 只列不改；AUTOMATION GUIDE 双语函数说明、services-sync:904 旧注释不动。阶段 4 收尾探针（2026-09-06）：全源+菜单加载后 commandp 且 supertag- 前缀共 124 条；仍被 triage 标"删"的 8 条全部已归属（conflicts-resolve/use-ours-all/use-theirs-all→阶段 6 K；act/act-dwim/act-mode/embark-act/embark-act-dwim→阶段 5 M，第 (6) 点），封存 13 条即本决定开头的 view-framework/helper→阶段 6 O，一次性 11 条（migrate-reciprocal-links 随 4f-1 摘 autoload 离开加载链，12→11）→阶段 6；triage 未收录的现名（discovery-*、delete-link、query-build/describe-syntax、text-link-refresh、menu 包装）均为阶段 2-4 改名或新增。阶段 4 关闭。
15. 阶段 5 M 探针结论与雏形（2026-09-06）：Embark 改为可选依赖（推荐，README 注明，defcustom supertag-embark-integration），覆盖 MODEL:267 的必需依赖；机制：绑进类型 keymap 的非命令函数由 Embark 以目标字符串调用、在目标窗口同点位执行且不经 minibuffer 注入，命令则会被注入并自动提交，故动作一律为非命令适配器、在点位重识别对象；每对象一个 Embark 类型（node/tag/link/concept/node-reference/region），keymap 无 parent，不改 embark-org keymap，Org 原生对象用 embark-cycle 切换；5M-1 雏形：ui-act 与 ui-embark 合并为 supertag-embark.el，act/act-dwim/act-mode/embark-act/embark-act-dwim 与二层菜单、目标缓存退役，动作见 README；留待项见决定 18。

16. 阶段 5 N 探针结论与雏形（2026-09-06）：superchat 为可选依赖（推荐，README 注明），不进 Package-Requires，运行时 fboundp 检查；机制（源码+隔离探针）：`superchat-runtime-submit` 是唯一外部入口（异步；`:prompt` 与活 `:buffer` 必填；`:delivery-function (response status tape-id)` 在 completed/failed 两终态必调；`:system-prompt` 成为 system message；`:tools 'none`、`:record-conversation nil` 不进会话历史；每次 Run 写 superchat 自己的 Tape sqlite（`superchat-data-directory`），backend 未配置走 failed 而非抛错）；插件规范禁止 blocking 入口与 `superchat--` 内部符号；5N-1 雏形：新特性文件 supertag-ai.el（提示词 alist `supertag-ai-prompts`、命令 `supertag-ai-extract-properties`、常驻隐藏 host buffer、JSON 候选契约 `{"NAME": {"value","source"}}`、内存候选表不持久化、Node View "Property candidates" 节逐条 Accept/Skip 与 Accept all/Discard all，接受经 `supertag-service-org-set-property` 保存并投影，跳过零写，调用时确保节点 ID），Embark 节点键 x，主菜单 e；标签集合批量提取、跨节点精确计划预览与取消进行中的 Run 已由 5N-2 完成（决定 24）；其它召唤（释义草稿/问答）仍留待，真实模型的 JSON 遵从度与提示词调参由用户在自己的配置下验证。 新 ID 标题按 add-tag 先例从活 buffer 经 supertag-node-sync-at-point 投影而不 save；文件级位置、未访问文件或磁盘上不存在的文件在补 ID 前零写入拒绝。 继承缺口（归模块 A/阶段 6）：set-property 对已在活文本但未保存的值按 no-op 返回，调用者无法区分'已持久'与'仅在活文本'，remove-tag/replace-tag 有 repair-projection 参数而 set-property 没有；5N-1 在 AI 层以磁盘核对代替，不改 writer：接受前读活 buffer 现值与审阅基线核对，基线已变则标 :stale、卡片显示 changed since extraction、要求再审并中止 Accept all；写后从磁盘文件重读该属性核对候选值，磁盘未见则保留候选并提示保存后再接受（Store 投影不作持久证明）；响应只接受顶层 JSON 对象（围栏优先，其次整文本顶层类型，无方括号包围时才取花括号回退）；来源引语经空白归一后对照已发送正文核实，未核实的标 unverified 不作引文显示；delivery 以每次请求的 token 核对，丢弃或重发后到达的旧响应忽略。 磁盘核对只认真实 PROPERTIES 抽屉中的 ID（源码块/正文中的 ID 文本不算），无真实匹配或多标题歧义返回 nil 保留候选。 继承缺口（归模块 A/阶段 6）：共享定位器 supertag-node-location--id-property-position（supertag-service-node-identity.el:54）以首个 :ID: 正则命中定位，源码块/正文中的 ID 文本会让活 buffer 写链（--with-node-buffer→set-property）误定位到前面的标题；5N-1 不改共享定位器，只在 AI 只读 reader 内隔离。

17. 阶段 5 G 语义候选层雏形（2026-09-06）：superchat 无 embedding API；参考 chai 的 Ollama `/api/embed` 形状，但按节点嵌入而非段落。新特性 `supertag-semantic.el`，默认关闭（`supertag-semantic-enabled=nil`），独立兼容 HTTP 端点默认 localhost:11434、模型默认 bge-m3、异步 curl，单进程、临时请求文件、字符预算批次与空闲计时器。输入来自 Store 的标题、大纲路径与前 1500 字自有正文；只读 Store、不改共享 writer/定位器、不写 Org 或自动建链。归一向量以 int8 字节存入独立 side-car，记录文本 hash、模型和维度；模型/维度不兼容重建；旧 :store-changed 主题只订阅一次，按节点路径标脏、删除时移除向量，不双订阅 canonical change。端点失败或 Stop 暂停后，仅节内 [Retry]/rebuild 可恢复，渲染不解除暂停；token 丢弃迟到响应。Node View 在 mention 后自动显示 Similar notes (candidates)，排除自身和已有出入引用；默认阈值 0.4、最多 5 项，原文前三行预览明确为节点级而非段落命中，链接打开原笔记、[View] 查看候选，不提供 [Add link]。维护菜单提供 rebuild/status/stop，README 双语说明 opt-in 与本地模型。 重新 opt-in 首次使用调和关闭期间的新增、修改和删除；成功批次仅在含当前节点自身向量或脏集合排空时刷新，保留阅读位置。side-car 在排空或距上次保存超过 supertag-semantic-save-interval（默认 30 秒）时写入，Stop/错误不写；标题用真实按钮按原 ID 跳转。继承缺口：supertag-view-node--activate-links-in-buffer 正则失效（view-node:518–522，HEAD 同），无消费者，归模块 G 阶段 6 处理。 5G-1 复审通过后另留待 5G-2 的测试补强：阅读位置用例改用非顶部 window-start；保存间隔到期分支加受控时钟用例。

   Leader 合成中英文 10 笔记探针（不是用户真实库；时间含模型加载）的依据：

   | 模型 | 10 条批量 / 单条 | 维度 | 同题最低 / 异题最高 | top-2 同题 / 中英互检 |
   |---|---|---|---|---|
   | dengcao/Qwen3-Embedding-0.6B:Q8_0 | 1.32s / 0.05s | 1024 | 0.543 / 0.463 | 8/8 / 通过 |
   | bge-m3:latest | 14.95s / 0.05s | 1024 | 0.503 / 0.545 | 7/8 / 通过 |
   | quentinz/bge-small-zh-v1.5 | 13.70s / 0.03s | 512 | 多项倒置 | 中英互检失败 |

   中文为主的库推荐改用 Qwen3-Embedding-0.6B；不把合成表现当真实质量保证。5G-2 留待：真实库中文质量与阈值校准、段落级命中和定位、[Add link]、进一步取消/超时策略、与 discovery 的关系。5G-1 测试只用合成 Store/临时 Org 与可选真实本机端点，不用用户笔记。

18. 阶段 5 M 收尾（2026-09-07）：探针确认 Embark 非命令适配器在目标窗口原点执行、活动选区仍保留；只读 containing-node 解析不补 ID，旧 UI containing-node helper 会补 ID，finder 不使用。① 可写 Org 正文回退为所在节点，标题 bounds/标题 label，保留原点执行；具体标签、id 链接、概念/投影对象优先，无 ID 也可识别但不写入。② 首标题前不提供 Supertag 对象，交给原生 Embark，文件级节点不作情景对象。③ 选区仅限可写 Org：RET/l add-link、t 逐节点 add-tag、p Promote 所选文字，全部复用既有命令及其跨标题/文件支撑校验。④ 投影视图 node-reference 的 RET 回原文、v 打开 Node View，不增加远程写动作；Node View 标签行以 type=:tag 与 tag-id 识别为当前节点的 tag，沿用 RET/r/c/R/D 与 Org writer，订阅刷新；语义卡标题补 supertag-node-id，Stream/Discovery 局部按键不变。⑤ 六类型 label 与非命令适配器契约保持，无新命令、别名或 embark-org keymap 修改。N 的提取属性键 x 已在 5N-1 完成。留待 5M-3：投影视图远程写动作、Stream 标签 token、文件级节点对象（见决定 23）。复审（2026-09-07）必修 0 项，另记两项：S1 tag 的 R/D 经非交互路径调用旧全局命令时预览 buffer 生成但确认前不显示（5M-1 继承，供 display 用 called-interactively-p 判断），后续修复须让预览与执行共用同一采集结果，不把适配器改成命令；S2 测试待补 Node View 标签行 c/R/D 回归与 view-tag finder 在其它 mode 返回 nil（阶段 6 测试补强）。

19. 阶段 6 K（2026-09-07）：Git 收缩为 Org 文本同步，保留 setup/clone/sync-now/sync-mode 四命令与异步串行传输。① 只 stage *.org 与根 .gitignore，DB/备份/锁/presence 不传输，driver/.gitattributes 配置退出，merge/conflicts 及专属测试封存。② 根来自唯一 sync root，与 DB 路径解耦；不迁移数据库、不改 tombstone，root 内的缓存由 ignore 排除。③ 旧仓跟踪 DB/attributes 时 setup 一次确认后 rm --cached 并提交，文件/历史保留；sync-now 只提示。④ clone 后始终从 Org 重建并保存本地投影，保存受既有 persistence guard 约束，失败不报告已完成。⑤ merge 成功按 ORIG_HEAD→HEAD 的 Org 文件集入既有队列，删除沿用 verify-file-nodes→node-mark-deleted-from-file：按文件查询为空、:file=nil/:orphaned-at 非 nil，GC 不变（leader 澄清）。⑥ 冲突保存 vault 状态、取消 pull/debounce timers、lighter !、首文件 smerge；显式 sync-now 在保存且无 marker 后完成 merge、重扫、恢复与 push，冷启动 unmerged 同样暂停，不自动选边。⑦ 保留 scoped staging、外部暂存保护、offline commit/catchup、push 拒绝后一次 fetch/merge/retry、三个时间配置、single-root 与 symlink 边界。⑧ 不改 sync 解析/哈希与共享 writer；doctor 仅更新 Git 契约与移除 DB 冲突 section。留待：多库并行同步归 L、冲突时其它脏状态保存策略、Org 冲突 UI 打磨。 复审三轮（2026-09-07）必修 R1–R5/R4b 全部关闭；另记：index 快照恢复对并发外部改 index 或崩溃非原子；异步 sentinel/GUI/Emacs 29 未测；冷启 doctor 用例未带。

## 工作规则

- 主仓 `/Users/chenyibin/Documents/emacs/package/supertag`，branch `supertag-refactor`；不动 `.worktrees/`；`sueprtag-backup/` 已于 2026-09-06 移到仓外同级目录 `/Users/chenyibin/Documents/emacs/package/sueprtag-backup/`（`mv`，可原样移回）。
- 不 commit、不 push、不安装；不覆盖或回滚继承的未提交修改；移动文件用 `mv`，不 `git mv`。
- 删除或封存前记录消费者（运行、动态、配置、持久化）；代码名陈旧或无静态调用不足以证明可删。
- 小而内聚的模块；先复用再新增；不留半成品分支。
- 文件按特性竖切，不按技术层横切（2026-09-06 已定）：阶段内碰到哪个模块，就把它散在 core/ops/services/ui/view 前缀里的文件合并成一个特性文件，文件内部用节分层，头部写明入口命令和依赖的横切模块。横切只保留 core-store / core-persistence / services-sync、service-org、view-framework。函数体内不再 `require`，依赖写在文件头。不单独做整仓搬家。
- 回报固定格式：改动（文件:行）、测试命令与结果、未处理项。

20. 阶段 6 M（2026-09-07）：新特性 `supertag-migrate.el`，数据版本 7.0.0。先核对快照字节，DB-only 步骤成功后最后盖版本并保存；error 从快照回载，5.0 前格式提示先用 6.x。root/global/embedded 字段与 association 原始来源进入 `:legacy-fields`，无法解释的值保留 raw；继承路径进入 `:legacy-extends`，缺父/成环只报告；nil 注册项删除，重复/无效只报告。四命令 run/preview/apply/status：文本迁移预览确认后复用既有 writer，字段完成须磁盘与投影均证明；继承改名接 3b，重键待迁移 tag 引用并删除已完成记录，冲突留存。pending parent/child 与字段引用排除孤儿清理，先 apply 再清理。启动只在版本迁移成功当次提示待导出数量。三旧迁移文件封存，五 field 集合/API/回滚及旧持久化守卫退役；未知根保留、旧根残留由 status 报告。6M-1/6M-2 分片冻结，独立验收另行进行；不承诺跨文件原子提交或崩溃恢复。 复审四轮（2026-09-07）：6M-1 R1（孤立 provenance 按 node/field-id 承接）、S1、6M-2 R2（改名后落盘失败的重试续做，nil 与 signal 都恢复精确待办）全部关闭；另记：三条故障时序共用一条回归用例，signal 三路径尚未入 checked-in 测试；nil 分支恢复后再 catch 会二次恢复（幂等无害）；不承诺跨进程/崩溃原子恢复。
21. 阶段 6 L（2026-09-07）：多库切换先保存当前库，配置 watcher 守卫拒绝未授权路径变化；切换统一清理 UI 候选、Automation 队列、scheduler、sync deferred、AI 候选与迁移诊断。scheduler/discovery 的状态文件按当前数据目录动态计算；Git 同步模式开启时拒绝切库；已打开的 Node View 在新库缺少旧 ID 时刷新为空态。每库只使用自己的 Store、历史与状态文件，不读取另一库快照；auto-switch 不重复加载已打开 buffer。`vault-test` 锁定保存失败、守卫、隔离与清理契约。 复审四轮（2026-09-07）：R1 保存被 guard 拒绝时按 durable 判定、R2 scheduler 先保存后停止/JSON 用 json-parse-string 显式 hash-table/新库重注册、R3 discovery 历史切库前清空切换后加载、R4 automation processing-timer 取消，全部关闭；另记：vault 套件 scheduler 往返仅单库路径，建议补真实双库 activate 用例锁调用顺序；多库 Git 不支持（单 sync root）；auto-switch 只在打开 Org 文件时触发；Discovery/Stream 打开中 buffer 的切库行为留待；worker 曾在主仓根留下 10 个 .elc 致两例失败，已删除，byte-compile 门只在临时副本执行。
22. 阶段 6 O（2026-09-07）：封存 13 条命令按真实消费者分三档。(a) 保留为当前能力 2 条：`supertag-view-refresh`（Stream `g`、Node View/Discovery 刷新、Runtime 回调都经它）、`supertag-view-style-mode`（org-mode-hook 经 `supertag-view-helper--auto-enable` 自动启用，SVG 标签读该 mode 变量）。(b) 删除 11 条：framework-init（root init 未调用）、dsl-example、config-save-to-store、list-interactive、select-and-render、select-from-schema、style-refresh、style-toggle、widget-mode、widget-forward、widget-backward，无 defalias。(c) 只删仅由 (b) 可达的链：widget-mode-map 与 field-map 导航键、DSL 适配器、`supertag-view-define-from-config`、context/schema 选择与列表链、`supertag-view-list`/`-for-tag`、`config-restore-from-store`、两个死 getter 与旧 declare。必留集合：view register/get/open、`--cleanup-instance`/`--refresh-instance`、`supertag-view-config-register/get/list` 与 `supertag--view-configs`（ops-tag 孤儿标签扫描读 config-list）、全部 widget 类型与 render-tree/selection、`supertag-view--read-tag`（Stream 交互入口）、helper 的标签写入/定位/font-lock/face/绘制。framework 约 909 行、helper 约 804 行；helper 的 defvaralias 与悬空 autoload cookie 删除；scheduler `(require 'json)`、service-org 的 template require 提到文件头；ui/service-org/framework/helper 补 Commands/Dependencies 头。
    测试与门：`view-framework` 默认套件 22 例（退役命令 unbound、Stream g/Node 改题/Discovery 新节点三条 Store 级夹具经真实渲染器刷新、org-hook 自动启用契约、config-only 标签孤儿生命周期、Stream 交互入口）；`vault` 套件加 stub 顺序例与真实双库 activate 往返（persist 先于 stop）；默认清单 33 套件，新增 automation-actions、extractor、persistence-restore、mention-extra（test-concept-mention、test-denote-reference 两份 ERT 文件；test-completion-newtag 为 kill-emacs smoke 脚本，fix10 移出）、node-view-extra（text-link-node-view、test-file-node-display）；`--guidance` 增 `test/static-gates.sh`（根无 .elc、非 archive 无 defalias/defvaralias、根 .el 均有特性头、无函数体 require 除 `static-gate: allow` 两处、`emacs -Q` 临时目录冷加载且 load-history 不含 /archive/；fix10 修正单 --eval 双 form 只执行 setq 的假绿，fix11 用 SUPERTAG_DEPS_LOADPATH 或 ~/.emacs.d/elpa 的 ht/dash 目录逐个 -L 解析 Package-Requires 依赖（ht 自身依赖 dash），不 package-initialize，并锁定主仓与 Emacs 之外只允许 ht/dash 加载；fix12 把子串白名单改为对每条 load-history 取 `file-truename` 的身份判定：主仓根下且含 /archive/ → 失败，主仓根、Emacs 根（data-directory 上级）、或 `file-name-base` 为 ht/dash 的依赖目录下 → 允许，其余「unexpected load」失败；leader 反例：外部 `supertag/hidden.el` exit 1，straight 布局经 SUPERTAG_DEPS_LOADPATH PASS，主仓 PASS）。README 双语：前五分钟改为 Stream/Node View/Discovery/org-capture/Embark/migrate，108 行 defcustom 配置表（leader 以 /private/tmp/supertag-phase6o2fix13-gen.el 冷加载求值生成：变量/默认值/用途=docstring 首句，按文件分组，路径默认值写作 `<data-directory>/…` 占位；support 逐名核对与根源码 108 个 defcustom 无缺失），API 段只列 query/node/schema/catalog/json，写入经 Node View 与 Org writer；六处旧流程（看板拖拽/仪表盘/可编辑字段/自定义视图）改写为真实 Stream 键 n/p、e + C-c C-c、v、g；Schema 原子改名承诺去掉，改为「预览确认后复用 Org writer，逐文件写入」；Ontology Policy/LLM Tools/as code/migrations 四节移入 doc/ARCHIVED-ONTOLOGY.md 与 _CN.md（带归档说明），现役 plain-data API/概念提及/情景动作三节从 fix13e-before 逐字恢复；原 doc/ 27 篇中 25 篇加归档说明，ABOUT-QUERY-BLOCK 两篇为现役文档、语法整理留待，另新增 ARCHIVED-ONTOLOGY 两篇（现 29 篇）。
    计数口径：run-tests.sh 逐套件跑 ERT，每套件各自汇总，无整体计数；"28/28" 是末位 Git 套件的条数，不是全量。support 独立复核（6O-1 三审版本）：33 套件 619 项，616 expected/3 skip；6O-2 终审版本（fix9 后）：32 个 ERT 套件汇总 621 项，618 expected/3 skip；包装器 exit 0 不覆盖套件早退，须看每套件 Ran 行。
    历史测试处置（65 个非默认文件，探针 B-test-disposition）：留记不改。归档依赖已退役 API 31（aggregate、architecture-boundary、archived-view-entrypoints、demo-aggregate/formula/reference、embed-cache、force-reload、formula、legacy-entry-retirement、migration-preflight、node-view-capabilities-retirement、perf-benchmark、query-block、quick-test、reciprocal-migration、reference、schema-time、tag-merge、tag-path、test-add-reference、test-embed-insert-block、test-field-node-reference、test-ui-act、test-view-kanban/table、view-refresh-single-path、view-schema-impact/runtime、virtual-column-benchmark/test）；待用户裁定原 8，其中 view-framework-test 已迁入默认清单，余 7（automation-condition、canonical-change、migration-property-field、query-library、retired-field-propagation、test-ui-search-performance、view-runtime）；断言混合失败 4（document-command-ownership、node-ops、tag-membership-org-first、test-back-to-heading）；夹具/清单 6（demo-virtual-column、diagnose、document-fixture、legacy-field-fixture、ownership-fixture、renovation-suites）；smoke 脚本 3（test-automation-scheduled、test-completion-newtag、test-inline-tag-filter，含 kill-emacs，exit 0 不算通过）；当前行为测试 13 中 12 已进默认清单，test-view-node-runtime 留记未纳入；归档依赖 31 是留记分类，不是可整体删除的清单。
    复审留记：6O-1 三轮（R1 误删 `supertag-view--read-tag` 已恢复、R2 吞错已去、S3 cookie 已删；S1 冻结 before 非真实前置字节、task.diff 整文件视作新增，以 support 独立副本为证）；6O-2 R2/R3/R4/R5 关闭，R1 migrate signal 四路径（首次 marker save :545、cleanup :552、retry 预跑 :620、apply-extends 重试 :508，其中只有 :552/:508 经 `--save-with-restore` 恢复分支）由 support 起草、fix9 逐字落地并关闭：migrate 24/24，删除恢复分支恰 (b)(d) 失败；探针同时触达生产缺陷 apply-extends 在普通 dolist 内 `cl-return`（void-variable），已改为同 row 内 if/progn 互斥分支；fix9 worker 冻结 diff 为空，以 leader 用 support 未改副本生成的 supertag-phase6o2fix9-leader.diff/.sha256 为基线；Store 级夹具替代磁盘+同步链按 leader 裁定接受；`(field …)` 查询语法文档留待。R5 文档经 fix13b–fix15c 七片逐项落地（worker 多次回报未落地或误切现役段，改为 leader 逐行 old→new 工单并在主仓逐条核实）；R5 冻结以 leader 生成的 /private/tmp/supertag-phase6o2-r5-cumulative-leader.diff（fix13-before → 主仓 README 两文件，两份 ARCHIVED 相对 /dev/null）与 -leader.sha256 为准，worker 的 r5-cumulative.diff 为空；复审五（support 临时副本正反 patch 往返 ROUNDTRIP_PASS，manifest 5/5 OK）判「无必修、可关闭」，6O-2 关闭，阶段 6 关闭。
23. 阶段 5 M 留待收尾 5M-3（2026-09-07）：① 投影视图 node-reference（Stream/Discovery/Node View 引用卡）增远程写动作 `t` add-tag、`r` remove-tag，经 `supertag-service-org-add-tag/remove-tag` 写入源文件，视图由订阅刷新；写前经 `supertag-embark--require-saved-node`：源文件不可读或节点未知拒绝，源文件另有已修改 buffer（非当前 buffer）拒绝，并在读入/确认之后、任何 tag-create 或 writer 之前再核一次（tag-change 同）。add-tag 剥 `=` 字面前缀后 sanitize，已有 tag 直接加，不存在则确认后 `supertag-tag-create` 再写，与 `supertag-add-tag` 同路径。② Stream 标签 token 成 tag 对象（`:origin :stream-tag`，bounds 为 `#tag` 文本段，node-id 为所在条目），沿用 tag 适配器 RET/r/c/R/D。③ 文件级节点仍不作情景对象（决定 18 ② 保持）；留记生产缺陷：level-0 节点经 service-org add-tag（service-org:1394–1436）把 `#+FILETAGS` 写在顶部抽屉之前、返回 nil 且 Store tags 为 nil，待模块 N/O 之外单独处理。④ 决定 18 留记 S1 已修（tag R/D 非交互路径预览始终显示），S2 测试已补。测试 embark 套件 30 例（无 Embark 29+1 skip，带 Embark 30/30）。复审二轮（2026-09-07）R1 bounds cadr、R2 prompt 后再守卫、R3 `=` 前缀、R4 来源可读预检全部关闭，无新必修。
24. 阶段 5 N 留待收尾 5N-2（2026-09-07）：① 取消为独立 cancelled 状态，提供 Cancel/Retry/Discard 与 Show raw；重试沿用原提示词，仓内不新增 LLM 客户端。② 按标签成员串行提取，仅本批次一次一个请求；已有独立 pending 请求可共存并跳过，拒绝第二批次。提交前占有 current，按批次身份非递归推进；取消与 delivery 的终结通知统一携带原 token/batch，核同一批次、current 槽位、请求 token 与终态后才推进，外部回调或 refresh 重入不能推进替代请求。③ 完成后打开 *Supertag AI Plan*，按文件/节点展示 current → value，k 跳过、a 应用、q 退出；node-id/name/value/current/file 字符串独立快照，应用前核候选值、原值及源路径，计划变化不写；源文件有未保存草稿的条目跳过。实际写入复用 supertag-ai-accept 与 Org writer，不自动应用。单节点 Accept 可能连带保存无关草稿的既有行为未在本片改变。④ vault reset 在任何取消回调前清 batch/candidate token 并使旧计划失效，旧 pending 请求尽力取消，失败不恢复旧状态；旧响应不能占用新批次。⑤ 菜单 AI E/C/B 与双语说明已落地；真实模型 JSON 调参、其它召唤仍留待，语义层不并入本片。
    验收：主仓标准默认入口 33 个 ERT 汇总均 0 unexpected，AI 50 项（49 通过、1 可选 Runtime 跳过）、view-framework 22/22；独立原探针 6/6、取消返回 2/2、同步 delivery/refresh 组合 1/1；guidance、临时副本编译与冷加载通过，仍有 obsolete/docstring/menu 声明警告。R1/R2/R3/R1b/R1c 与 S1/S2 经独立复审全部关闭，末轮无新增必修/建议，leader 据此关闭 5N-2。五文件累计冻结 SHA256 56306ca298c092232ed3aa3d4bb5ad09dc98e92bb4c64202f76c1e089744057c（5n2-before → a/b/c 与全部修复），往返重放及主仓五文件哈希通过；PLAN 本条不在该源码冻结内。边界：Emacs31 合成回调/refresh/reset seam，未验证真实 Runtime/模型、GUI 或完整双库切换；先前 sandbox Git 失败与标准入口 Git 通过作为分环境证据保留，原因未定位，不归因本片。

25. 阶段 5 G 产品收缩（2026-09-07）：用户明确不保留语义卡 `[Add link]`，直接跳转原文后使用已有 `supertag-add-link` 更方便。语义卡只负责候选推荐、原文预览、标题跳转与 `[View]`，不承担 Org 写入；保留 5G-2a 的 timeout/Stop/Retry/上下文调和及 5G-2b 的段落提取/定位。撤回未验收的 5G-2c 按钮、四个专属 helper、两项新增直接依赖与六个专属测试，恢复无该按钮的只读断言；普通 add-link、共享 materializer/保存/投影/显式 retry 不变，不新增开关或兼容别名。撤回原因是产品边界，不把审查 R1 的确认 freshness 缺陷称为已修复，也不扩大修改共享 writer 的后续草稿 retry 语义。旧冻结与审查记录留作历史证据，本条覆盖旧 5G-2c 工单范围；双语配置说明与真实库校准等仍待，不据此关闭整个 5G-2。

26. CLEANUP-CAPTURE-1（2026-09-07）：按用户决定退役独立 Capture 引擎，记录统一使用标准 org-capture，不另造模板/目标选择/插入系统。supertag-services-capture.el 从 919 行缩至 394 行：删除 23 个无现役消费者的函数、三个旧配置与一个会话状态，清掉专属直接依赖和失效节标题，不留兼容别名或隐藏开关。九个存活函数的签名与函数体逐项比对不变（两处 docstring 去旧 DSL 描述）：普通标签添加/替换/事务/保存投影/失败补偿保留；org-capture 的 opt-in hook、默认关闭、身份收尾、标签提示、Org 属性及 :field 兼容输入、既有 move 处理保留。标签 helper 暂留原文件，不随本片搬家；Promote 模板与普通 add-link 不变。双语 README 删除三项失效配置；身份测试用普通 Org 文本替代已退役插入器，并新增七例；非默认 write-efficiency 只删三条旧引擎专属测试，共享定位和其它历史契约保留。
    验收：worker 正式 identity 红 18 项 16 expected/2 unexpected，命中退役函数与变量仍绑定，生产 before cmp=0；最终 identity 22/22。leader 独立核查五文件、九个保留函数体及零运行残留，通过原三条标签保护 before/after 各 3/3、共享定位/命令退役控制 3/3、guidance、临时副本编译与默认完整 33 套件（703 项=700 expected+3 optional skip，0 unexpected）。真实 org-capture→finalize→hook/身份/标签 writer→投影及重建测试通过；未测 GUI/用户配置/真实库/Org 版本矩阵，未运行真实模型或 Runtime，保留编译警告及 sandbox/native Git 分环境说明。leader 据此关闭本片，不关闭整个简化工作；ops-batch、Canonical Change、标签代码归属及其它候选另行排片。

27. CLEANUP-BATCH-1（2026-09-07）：退役无仓内现役调用的通用 CRUD 批量包装层，删除 supertag-ops-batch.el 及 supertag.el、supertag-services-sync.el 各一条 require；三个旧 Lisp API supertag-batch-create/update/delete 不留兼容别名。两份存活生产文件其余字节逐项比对不变；core-transform 事务宏、九个单项 node/tag/relation CRUD、Store 回滚与事件、同步队列、AI 批次及标签批量操作保留。本决定明确放弃旧 API 兼容，不代表私人 Automation 规则、持久化函数/闭包或第三方不存在消费者；这些外部来源未读取，是否引用仍未知。
    验收：新增两条默认 contract ERT，分别真实冷加载同步服务与主入口，隔离临时路径，绑定 after-init-time=nil，所有出口清 startup/kill/Org hooks；先确认入口加载成功，再断言退役 feature/API/load-history/文件缺席及事务宏、九个单项 API 保留。worker 正式红 contract 22 项=20 expected+2 unexpected，独立审查在 before 源码副本复现两条目标红，均非 setup 失败；主仓 contract 22/22、saved-projection 21/21、tag-change 13/13、AI 49+1 optional skip。leader 独立 before/after 冷加载与真实事务提交/回滚控制通过，完整默认 33 套件 705 项=702 expected+3 optional skip、0 unexpected；guidance、diff-check、临时副本编译通过，worker before/after 各 60 条编译 warning 消息一致，不称零警告。现有 Herdr worker 实施、support 独立审查无必修/新增建议，leader 据此关闭本片；未提交或暂存，未测试真实 Runtime/模型、用户配置/真实库、GUI/版本矩阵，保留 sandbox/native Git 分环境边界。Canonical Change、标签代码归属及其它候选仍待另行排片，不关闭整体简化。
